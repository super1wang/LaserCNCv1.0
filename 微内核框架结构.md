# LaserCNC 微内核框架结构

审阅日期：2026-05-27  
本文描述当前代码中的实际框架结构；细节约束仍以 `微内核架构框架.md` 和 `代码规范.md` 为准。

## 当前架构定位

LaserCNC 已收敛为单项目、三数据域、唯一 workspace view 的微内核式桌面应用：

```text
QApplication / main.cpp
        ↓
Kernel
├── AppSettings
├── LcncProjectManager
├── TaskManager
├── ServiceRegistry
├── EventBus
└── ModuleRegistry
        ↓ init/start by dependency
CadModule → CamModule → ProcessModule
        ↓
MainWindow + CommandContainer + GuiApplication
        ↓
workspace GuiDocument
```

核心原则：Kernel 管生命周期和服务定位，不承载业务状态；业务状态由 ProjectManager、模块 service 和 view registry 明确持有。

## 分层依赖

```text
app / UI / commands
      ↓
modules facade / services
      ↓
view
      ↓
core/algorithms
      ↓
core
```

当前检查结论：

- `core/**` 未发现 include `view/`、`modules/`、`app/`。
- `view/**` 未发现 include `modules/`、`app/`。
- 旧入口 `Kernel::current().app()`、`LcncApplication`、`activeDocument()`、`machineGuiDocument()` 未在 `src/**` 中出现。
- `GuiDocument` 仍保留少量兼容 wrapper，例如裸 entry 版本 `aisShape()`、`eraseEntity()`、`rebuildDisplay()` 和 `sourceDocument()`，后续应继续收敛到 document/domain-aware API。

## Kernel 核心

| 对象 | 所有权 | 职责 |
| --- | --- | --- |
| `Kernel` | `main()` 栈对象 | 进程级唯一入口；注册核心对象；启动和关闭模块；提供 `Kernel::current()`。 |
| `AppSettings` | Kernel 直接拥有 | 应用配置、渲染 profile、模块禁用配置。 |
| `LcncProjectManager` | Kernel 直接拥有 | 唯一项目生命周期和三域 document 所有权。 |
| `TaskManager` | Kernel 直接拥有 | 后台任务调度和进度通知。 |
| `GuiApplication` | `main()` 创建，Kernel 裸指针引用 | view 层入口，拥有唯一 workspace `GuiDocument`，避免 core 反向依赖 view。 |
| `CommandContainer` | MainWindow 创建，Kernel 裸指针引用 | QAction/命令容器，供模块命令注册和 UI 查找。 |
| `ServiceRegistry` | Kernel 成员 | 模块 facade、Process 控制器、MachinePose 等服务注册。 |
| `EventBus` | Kernel 成员 | 跨模块事件基础设施。 |
| `ModuleRegistry` | Kernel 成员 | 模块依赖拓扑排序和生命周期管理。 |

允许的全局访问入口只有：

```cpp
lcnc::Kernel::current().projectManager();
lcnc::Kernel::current().guiApp();
lcnc::Kernel::current().taskManager();
lcnc::Kernel::current().appSettings();
lcnc::Kernel::current().service<CadModule>();
lcnc::Kernel::current().service<CamModule>();
lcnc::Kernel::current().service<ProcessModule>();
```

## 项目模型

```text
LcncProjectManager
├── LcncProjectSession
├── Workpiece LcncDocument
├── Machine LcncDocument
└── CAM LcncDocument
```

| Domain | Document | 项目状态 | 主要数据 |
| --- | --- | --- | --- |
| Workpiece | `workpieceDocument()` | `session().workpiece()` | 工件源几何、CAD 建模 shape、草图/特征结果。 |
| Machine | `machineDocument()` | `session().machine()` | 机台几何、轴分组、挂载代理、当前过渡期的运动学绑定。 |
| CAM | `camDocument()` | `session().cam()` + `CamDataManager` | 稀疏 OCC 轮廓和可选择 CAM 几何；密集刀路、轮廓 stable id、图层和工具关联 runtime 数据由 `CamDataManager` 管理。 |

规则：

- `LcncDocument` 是 OCC/XCAF 容器，不负责项目身份、工程路径或跨域业务逻辑。
- 项目名、项目路径、dirty flags、manifest 和 workpiece source path 归 `LcncProjectSession`。
- `.lcnc` 新建/打开/保存只走 `LcncProjectManager`。
- 普通 STEP/IGES/STL/BREP 导入只更新 Workpiece 域。
- Machine 和 CAM 不再创建独立可见 `GuiDocument`。

## View 和显示注册

```text
GuiApplication
└── workspace GuiDocument
    ├── GraphicsScene
    ├── AIS_InteractiveContext
    ├── V3d_View
    ├── RenderingManager
    └── Display registry: (DocumentId, XCAF entry) → AIS_Shape
```

`GuiDocument` 是当前公共视窗唯一管理者：

- Workpiece、Machine、CAM 数据都显示到同一个 workspace。
- AIS 对象使用 `{DocumentId, entry}` 定位，避免不同 document 中相同 XCAF entry 冲突。
- `rebuildDomain(ProjectDomain, LcncDocument*)` 只重建指定域，不清空其它域。
- `selectedEntries(DocumentId)` 和 `aisShape(DocumentId, entry)` 用于工程树、CAD/CAM 命令和选择同步。
- `RenderingManager` 仍通过 `sourceDocument()` 兼容旧路径；后续应改为直接按 display registry/domain 分组应用样式。

## 模块结构

| 模块 | id | 依赖 | 数据域 | 主要职责 |
| --- | --- | --- | --- | --- |
| CAD | `cad` | 无 | Workpiece | 工件导入/导出、建模、选择、草图、特征和 CAD 命令。 |
| CAM | `cam` | `cad` | Machine + CAM + Workpiece 只读 | 机台加载、轴标定、工件挂载、刀路生成、CAM runtime 数据和仿真渲染。 |
| Process | `process` | `cam` | 执行状态 | 连接/断开、仿真/控制器模式、运行、暂停、停止、回零、急停、倍率和轴位置。 |

模块接入方式：

- 继承 `IModule`，通过 `ModuleInfo` 声明 id、版本和依赖。
- 在 `init()` 中把模块自身和 facade contract 注册到 `ServiceRegistry`。
- `ModuleRegistry` 使用拓扑排序保证 `cad → cam → process` 启动，关闭时反向停止。
- 跨模块调用优先使用 facade/service contract；跨模块通知优先使用 signal 或 EventBus。

## ProjectExplorer

ProjectExplorer 是聚合视图，不拥有数据。

```text
Workpiece document snapshot
Machine document + kinematics snapshot
CAM document + CamDataManager runtime snapshot
        ↓
ProjectExplorerSnapshot
        ↓
QTreeWidget / future model-view adapter
```

当前边界：

- 根节点保持 Workpiece、Machine、CAM 三域独立。
- Process 执行页保留独立 tab，不进入工程树。
- 节点通过 `DocumentId`、entry、node type 和 stable id 路由到模块操作。
- Machine 节点显隐/选择限定 machine document；Workpiece 节点限定 workpiece document；CAM runtime 轮廓使用 CAM 数据 stable id，图层节点由 `CamDataManager` 快照生成且不由 ProjectExplorer 拥有。

## `.lcnc` 工程包

当前 `.lcnc` 是 zip 包，内部基础结构为：

```text
project.toml
project.xbf
```

当前实现路径：

```text
LcncProjectManager::saveProject/openProject
        ↓
LcncProjectPackage::save/load
        ↓
临时 staging 目录
        ↓
PowerShell Compress-Archive / Expand-Archive
```

已知需要优化：PowerShell zip 后端应替换为可测试、可恢复、可跨环境的 zip backend；`manifest.sourceFilePath` 保存仍需接入 `LcncProjectSession::workpiece().sourceFilePath`。

2026-05-09 更新：`manifest.sourceFilePath` 已改为由 `LcncProjectManager` 从 `LcncProjectSession::workpiece().sourceFilePath` 传入 package 保存流程；PowerShell zip 后端本轮暂保留，后续阶段先抽象 archive backend 和原子保存，再替换为 QuaZip。

## 启动和关闭生命周期

```text
main()
 ├─ QApplication
 ├─ Logger::init
 ├─ Kernel kernel
 ├─ kernel.registerCoreServices()
 ├─ AppSettings::loadDefault()
 ├─ GuiApplication guiApp -> kernel.setGuiApp(&guiApp)
 ├─ add CadModule / CamModule / ProcessModule
 ├─ kernel.bootstrap()
 ├─ MainWindow + CommandContainer
 ├─ app.exec()
 ├─ AppSettings::saveDefault()
 ├─ kernel.shutdown()
 └─ Logger::shutdown()
```

稳定性要求：

- Qt/OpenGL surface format 必须在 `QApplication` 创建前设置。
- `GuiApplication` 必须在模块 init 前注入 Kernel。
- MainWindow 生命周期应早于 GuiApplication 和 Kernel 结束。
- 模块关闭必须按依赖反向执行。

## 下一步结构优化方向

1. 从 `LcncDocument` 迁出 `MachineKinematics`，建立 MachineModelManager 或 MachineProjectState 管理器。
2. 将 `GuiDocument` 兼容 API 收敛到 domain/document-aware API，移除裸 entry 操作路径。
3. 将 `RenderingManager` 从 `sourceDocument()` 兼容逻辑改为按 display registry/domain 应用样式。
4. 拆分过重的 `CadModule` 与 `CamModule`，把导入导出、显示同步、机台模型、刀路 runtime 进一步 service 化。
5. 将 `.lcnc` package backend 抽象为可替换接口，增加原子保存、失败回滚和 package schema 校验。
6. 增加 smoke/集成验证：机台加载、工件导入、三域显隐、CAM 轮廓、保存打开、Process tab 独立。
