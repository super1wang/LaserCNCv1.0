# LaserCNC 代码规范（v2.3）

> 编码前必读。所有代码变更默认遵循本文与 [ARCHITECTURE.md](ARCHITECTURE.md)。

## 1. 架构边界

调用方向固定为：

```text
app / modules/*/ui / commands
    -> modules facade / services
    -> view
    -> core/algorithms
    -> core
```

硬性规则：

- `core/**` 不得 include `view/`、`modules/`、`app/`。
- `view/**` 不得 include `modules/`、`app/`。
- `core/algorithms/**` 不得依赖 `QWidget`、`QAction`、`QDialog`、`QMessageBox`、`LcncDocument`、`GuiDocument`、`Kernel`。
- 跨模块同步调用走 facade/service；跨模块通知优先走 EventBus 或 Qt signal，不互相 include 具体模块头。
- 进程内唯一全局入口是 `lcnc::Kernel::current()`。

## 2. 三域语义与物理存储

LaserCNC 使用三个逻辑域，但不是三份独立工程文档：

- 一个项目 `LcncDocument` 同时保存 Workpiece/Auxiliary 与 CAM 类型的 XCAF 实体。
- `CamDataManager` 保存稠密刀路、轮廓、图层和 CAM 工艺状态。
- `MachineWorkspace` 独立保存机台几何与运动学，不进入工程包。

规则：

- 普通 CAD 文件导入只替换 Workpiece document。
- `.lcnc` 工程打开/保存事务式处理项目 XCAF 与 CAM 数据；机台不随工程保存。
- `.lcnc` 归档保存必须先完成同目录 staging 文件，再原子替换目标；任何失败路径必须保留旧包并删除 staging 残留。
- 应用和库只可读取包含 `tools.toml` 的当前 v4；v1/v2/v3 与 `process_cutting_plan.toml` 必须明确拒绝，不得为 GUI 或库添加兼容回退、迁移或全局工具参数猜测。
- 项目专属资源必须通过 `ProjectPackageExtension` 在 core staging 事务中读写；不得由模块解压、重写或二次打包 `.lcnc`。工具参数必须从包内 `tools.toml` 以可逆的完整字段集恢复，不得只按全局工具名解析或静默丢弃未知项目参数。
- `.lcnc` staging 采用 `QTemporaryFile` 时，压缩器创建目标前必须让该对象离开作用域；只调用 `close()` 不足以保证 Windows 的 `MoveFileExW` 不会因共享冲突失败。
- 任何工程包写入失败都必须保持旧包字节不变；`lcnc_project_package_test` 以 SHA-256 断言该约束。
- 机台构型指纹不匹配不得仅记录日志：core 必须持有工程兼容状态并发信号，UI 必须提示，真实加工预检必须拒绝；仿真可继续用于诊断。
- 当前格式改动必须保持 v4 round-trip 和缺失资源拒绝测试；历史格式 fixture 必须断言明确拒绝，不能作为兼容入口保留。
- TaskManager 任务必须保持协作取消、有限等待和异常失败转换语义，并由 `lcnc_task_manager_test` 回归验证。
- Process `Service`、`MotionControl`、`LaserDevice`、`LDFactory`、`ProcessParameterRegistry` 及其 SimulatorCMHP/ACS/GTN/激光适配器的配置访问必须使用构造注入的 `ProcessSettingsService`；设置初始化失败时不得创建设备服务，也不得重新引入全局设置查询。
- ASan 验证目标必须自动部署 AddressSanitizer 与 OCCT TBB DLL；不得把 PATH、手工拷贝或本机残留 DLL 当作测试前提。
- 资源基线脚本必须在被测进程异常退出时保留 CSV，并允许对稳定段私有工作集/句柄增长设门禁；启动初始化样本不得直接判定为泄漏。
- `ProjectExplorer` 只聚合三域 snapshot，不拥有业务数据。
- 加工执行页独立保留，不进入 ProjectExplorer 节点树。
- 新代码不得新增同义兼容别名；访问活动视图使用 `activeGuiDocument()`，访问实体时显式携带 domain/`EntityKind`。
- all-off 构建中的 `Simulator` 必须是本地状态实现，不得解析或链接 ACS/GTN 等供应商 SDK。`SimulatorCMHP` 归属 ACS；其控制器头和实现只能由 ACS option-gated target 编译，公共 facade/DTO 不得暴露供应商类型。ACS Simulator 连接必须加载 CMake 部署的 `Simulator.prg`，缺失或加载失败即连接失败。
- 设备公共头不得包含 `MessageModule` 或旧日志头；临时兼容提示只能在具体 `.cpp` 中包含。
- 消息提示和设备兼容日志必须使用 `lcnc::Logger`；不得重新引入旧 `LogModule` sink 或独立日志路径。
- 模块内部必须优先使用构造/生命周期注入的设置服务，不得以 `ProcessSettingsService::current()` 读取配置。
- `ToolFactory` 查询不得通过 `operator[]` 产生空工具；同一索引的设置重载必须显式替换旧工具。
- ACS/GTN 的轴选择、扩展轴判断和仿真选择必须读取 ProcessModule 拥有的 `ProcessRuntimeConfiguration`；不得重新引入已删除的 `DT` 静态运行时状态。在该边界归一化、去重轴名并过滤 `BASE` 伪轴，相关改动必须保持 `lcnc_process_runtime_configuration_test` 通过。
- 设备连接 UI 只能调用异步全设备操作；不得重新引入同步单控制器连接接口或 `ProcessLayerJob::order` 兼容字段。
- 回零顺序、轴定义比较和仿真轴坐标等纯轴逻辑放入 `runtime/process_axis_utilities`；不得依赖 UI 或设备 SDK。
- 设备最终关闭必须复用 `Service::shutdownDevices()`；先停激光/红光，再停止运动与断开控制器。

## 3. 头文件与命名

- 头文件首行使用 `#pragma once`。
- 公共类、struct、enum、自由函数、公开方法必须有简短用途说明。
- 头文件优先前置声明，重型 Qt/OCC 依赖尽量留到 `.cpp`。
- `.cpp` include 顺序：自身头 -> `core` -> `view` -> `modules` -> `app` -> 第三方 -> 标准库。
- 文件名使用 `snake_case.{h,cpp}`；类/struct 使用 `PascalCase`；函数使用 `camelCase`；成员变量使用 `m_camelCase`；常量使用 `kCamelCase` 或 `UPPER_SNAKE_CASE`。
- 名称必须表达当前职责，避免继续使用 `workspace`、`projectDocument`、`documentTree` 这类已与三域语义冲突的旧名称。

## 4. 模块编排

`modules/<domain>/<domain>_module.cpp` 只负责：

1. module/service 生命周期注册。
2. facade 接口实现。
3. 调用算法或 service 并写回 domain document。
4. 状态机维护、信号转发和刷新请求。

禁止在 module 中堆放：

- 大段 OCC `BRep*`、`BRepAlgoAPI_*`、`XCAF*` 计算。复杂几何逻辑应迁到 `core/algorithms/<domain>` 或模块 service。
- AIS shape 构建和显示细节。显示对象归 `view/*_renderer`。
- 临时 `QDialog`、`QMessageBox`、`QFileDialog`。参数收集归 UI/dialog 或 app 对话服务。

## 5. 算法库

`core/algorithms/<domain>` 应保持可复用、可测试：

- 输入使用 OCC 类型、标量和轻量 POD 参数。
- 输出使用返回值、输出参数和可选 `QString* errorMessage`。
- 不抛业务异常；OCC `Standard_Failure` 由调用层捕获并记录日志。
- 优先自由函数，避免为了包装而包装类。
- 命名空间使用 `lcnc::cad_algo`、`lcnc::cam_algo`、`lcnc::kinematics`。

## 6. 命令与 UI

- 命令类继承 `lcnc::CommandBase`，构造函数完成 QAction 基本装配。
- `isEnabled()` 只做可用性判断，`execute()` 执行业务并返回 `bool`。
- 命令不直接弹业务错误对话框；错误通过 facade 返回或 signal 交给 UI 层展示。
- 命令文件按子域拆分，例如 `commands_file`、`commands_transform`、`commands_machine`、`commands_process`。

### 6.1 国际化（i18n）

- 所有面向用户的标题、按钮、提示、对话框和状态文字以英文作为源文本，并使用 Qt 的 `tr()` / `QCoreApplication::translate()` 包装；不得新增直接面向用户的中文字符串。
- 每个迁移后的界面或日志字符串都要在相邻位置添加 `中文翻译：...` 注释；代码注释保持中文，便于本项目维护；注释不进入翻译目录。
- 简体中文译文维护在 `translations/lasercnc_zh_CN.ts`。新增或修改英文源文本时，必须在同一变更中同步该文件，并保持占位符（如 `%1`）完全一致。
- CMake 会在构建时以 `lrelease` 生成 `.qm` 并嵌入资源；运行时根据 `[general].language` 加载。新增语言时，增加对应 `.ts`、构建资源和语言选项。
- UI widget/dialog/ribbon 只负责展示、参数收集和触发命令，不持有业务数据所有权。

## 7. ProjectExplorer 节点

- 节点树必须由 snapshot 派生，不直接遍历 UI item 作为业务事实。
- 节点身份使用稳定 id：XCAF label entry、domain id、CAM contour stable id 等；禁止把 `QTreeWidgetItem*` 或 row index 当长期业务身份。
- ProjectExplorer 可路由右侧面板，但不能修改 domain document 的所有权关系。
- Toolpath 轮廓选择短期可保留 `contourIndex`，新增代码必须同时规划稳定 contour id。

## 8. 渲染配置

- 应用程序选项中的 `RenderProfileSettings` 是 CAD/CAM 渲染配置的归属点。
- 旧 `MachineRenderQuality` 只能作为迁移兼容层存在；新代码不得扩散全局 alias。
- view 层消费渲染配置，modules/settings 不应反向依赖 view 的显示细节。

## 9. 日志与异常

- 调用形式统一为 `LCNC_<LEVEL>(lcnc::LogCode::Xxx, "fmt {}", arg)`，`LogCode` 必须全限定。
- `LCNC_DEBUG`：函数入口/出口、关键 return、复杂状态转换。
- `LCNC_INFO`：模块 init/start/stop、用户级里程碑。
- `LCNC_WARN`：参数非法、降级路径、重复注册、可恢复异常。
- `LCNC_ERR`：操作失败、catch 块、依赖缺失。
- `LCNC_CRIT`：不可恢复错误的 critical 日志；调用方仍必须显式返回、抛出或终止，不能假设日志宏改变控制流。
- 每个 `catch` 块必须记录 ERR 日志，禁止空 catch。
- 所有 `IModule::init/start/stop` 实现必须具备失败回滚；`stop()` 可被正常关闭、init 失败和 start 失败重复调用，且不得向外抛异常。

## 10. 资源与仓库卫生

- RAII 优先：`std::unique_ptr`、`std::shared_ptr`、OCC `Handle()`、Qt parent tree。
- 不在头文件使用 `using namespace`。
- 不提交 build 产物、IDE 索引、临时日志、`.user` 文件。
- 第三方依赖必须明确策略：vendored、FetchContent 或 vcpkg。未经确认不得混用。
- CMake 不应长期硬编码个人机器路径；必要时用 cache variable 或 toolchain 配置承载本地路径。

## 11. Process 设备访问

- ACS、GTN、激光器和 IO 的 SDK 调用必须持有 `ProcessDeviceCoordinator` 租约；UI、轮询、监控和 worker 不得跨异步边界缓存 `MotionControl*`。
- PureSimulation 只能由显式纯仿真模式进入；ACS、GTN 或 `SimulatorCMHP` 被选择时，连接失败、断连或未知后端必须返回错误，禁止回退 Pure。启用 ACS/GTN 的构建默认关闭 PureSimulation。
- 控制器轮询、环境 IO 轮询和串口外设轮询必须分别在专用单线程池执行；控制器可高频，串口外设不得高频。`QSerialPort` 不得归属 GUI 线程，GUI 线程只接收不可变采样结果和信号。
- Stop、异常和关闭路径必须先停止发新命令并关闭激光/吹气等安全输出；失败必须记录 `LCNC_ERR` 并保持 Error。Stop 是唯一软件安全停机入口，恢复必须经设备检查后才可回到 Idle。
- 供应商 SDK 头只允许出现在 option-gated 的私有实现；公共 facade、DTO、步骤接口不得泄漏供应商类型。
- 新增或修改 ACS/GTN/BDAQ/真实激光实现时，必须验证 all-off 与其 enabled preset；SDK 开关关闭时不得编译其 `.cpp`、包含其头或链接其 import library。
- 模块启动 `TaskManager` 任务时必须持有 task-id；`stop()` 先请求取消并等待其 worker，不得把借用指针留给已脱离模块生命周期的 lambda。
- 涉及所有权、异步、OCC 或供应商 SDK 的改动必须运行 ASan preset；资源基线由采集脚本记录。Application Verifier 只能在明确测试窗口内启用，并在测试后显式关闭。

## 12. 提交前验证

1. 使用 VS Code CMake Tools 构建 `LaserCNC`。
2. 运行分层检查：`core/**` 无 `view/|modules/|app/` include，`view/**` 无 `modules/|app/` include。
3. 运行旧 API 检查：新代码不得新增 `projectDocument()`、`sourceDocument()`、`workspaceGuiDocument()`、`ensureProjectDocument()` 等同义别名调用。
4. 运行 `ctest --test-dir build-cmake --build-config Debug --output-on-failure`；`architecture_checks` 必须通过。
4. 按改动域做 smoke：工件导入、机台加载、工件挂载、CAM 轮廓、执行页、`.lcnc` 保存/打开。

## 13. 速查清单

- [ ] 文件职责单一，命名与三域语义一致？
- [ ] 没有反向 include 或跨模块直接依赖？
- [ ] 复杂几何逻辑已抽到算法/service？
- [ ] 命令与 UI 对话框职责分开？
- [ ] ProjectExplorer 节点有稳定身份？
- [ ] 渲染配置走应用程序选项？
- [ ] 日志和异常符合规范？
