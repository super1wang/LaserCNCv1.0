# LaserCNC 代码规范（v2.2）

> **编码前必读**。所有 PR / commit 都假定遵循以下规范。
> 配套阅读：[微内核架构框架.md](微内核架构框架.md)、`/memories/repo/coding-standards.md`（精简版）。

---

## 1. 头文件

1. **每个公开接口必须有注释**：类、struct、enum、自由函数、公开方法均需说明用途。短句即可，不要堆 doxygen 模板。
2. **私有成员变量**：仅在含义不明显时加注释；命名 `m_` 前缀。
3. **include 顺序**：① 自身 .h（仅 .cpp 内）→ ② 项目内（按层次：core → view → modules → app）→ ③ 第三方（Qt / OCC / SARibbon / spdlog / toml11）→ ④ 标准库。组之间空行。
4. **前置声明优先**：头文件能 forward-declare 的就不 include，把重型 OCC / Qt 依赖留到 .cpp。
5. **Pragma once**：所有头文件首行 `#pragma once`。

## 2. 日志规范

| 级别 | 触发场景 | 示例 |
|------|----------|------|
| `LCNC_DEBUG` | 函数入口/出口/关键 return；服务注册查找 | `"CadModule::init begin"` |
| `LCNC_INFO`  | 模块 init/start/stop；用户级里程碑 | `"Module init: 'cam' v1.0.0"` |
| `LCNC_WARN`  | 参数非法、覆盖注册、命令未找到、降级路径 | `"ServiceRegistry: 'X' already registered"` |
| `LCNC_ERR`   | 操作失败、依赖缺失、异常被捕获 | `"Module 'cam' init threw: ..."` |
| `LCNC_CRIT`  | 不可恢复错误（同时抛 `std::logic_error`） | `"Kernel::app() called before registerCoreServices()"` |

**规则**：
- 调用形式：`LCNC_<LEVEL>(lcnc::LogCode::Xxx, "fmt {}", arg)`，`LogCode` 必须全限定。
- 正常发布构建默认 `info` 级；调试期可由 logger 切到 `debug`。
- **调试模式下**：函数入口 + 函数出口（含每个 return 分支）+ 异常 catch + 必要中间步骤都要打 DEBUG。
- **每个 catch 块必须打 ERR 日志**，至少含异常信息或上下文 ID。空 catch 不允许。
- **避免日志洪水**：循环内日志要么 DEBUG 级、要么聚合后再打。

## 3. 命名

| 类别 | 规则 | 示例 |
|------|------|------|
| 命名空间 | 全小写 + 下划线 | `lcnc`、`lcnc::cam_algo` |
| 类 / struct | PascalCase | `CamModule`、`LaserToolpath` |
| 函数 / 方法 | camelCase | `loadMachine()` |
| 成员变量 | `m_` + camelCase | `m_kinematics` |
| 静态/全局变量 | `s_` / `g_` + camelCase | `g_kernelCurrent` |
| 常量 | UPPER_SNAKE_CASE 或 `kCamelCase` | `kMaxAxisCount` |
| 文件名 | 小写下划线 | `cam_module.cpp` |
| 命令类 | `Cmd<Verb><Noun>` | `CmdRunStart`、`CmdMakeBox` |
| 命令 id | `<模块>.<动作>` | `process.runStart`、`cad.makeBox` |

## 4. 架构依赖铁律（v2.2）

参见 [微内核架构框架.md §4](微内核架构框架.md)。摘要：

```
ui ─► modules ─► view ─► core/algorithms ─► core
```

- `core/**` **不得** include `view/` `modules/` `app/`。
- `view/**` **不得** include `modules/` `app/`。
- `core/algorithms/**` 函数零 Qt UI 依赖；输入仅 OCC + 标量；不依赖 `LcncDocument / GuiDocument / Kernel`。
- 跨模块通信：facade 接口或 EventBus，禁止业务模块互相 include 头文件。
- 进程内唯一全局：`lcnc::Kernel`；其余原 `XxxClass::instance()` 全部移除。

## 5. 模块编排约定

业务模块 `modules/<x>/<x>_module.cpp` **只做四件事**：

1. **服务注册 / 信号转发**：init() 注册 IService、订阅事件、连接 Qt 信号。
2. **业务编排**：调 `core/algorithms/<域>` 算法 → 写回 `LcncDocument` → emit 模块信号。
3. **状态机维护**：模块自身的小状态（如 `ProcessModule::State`）。
4. **门面接入**：实现 `i_<x>_facade.h` 接口供跨模块/UI 调用。

**禁止**：
- module.cpp 直接持有大段 OCC `BRep*` / `BRepAlgoAPI_*` / `XCAF*` 调用 → 必须落到 `core/algorithms/<域>` 自由函数。
- module.cpp 持有 AIS_Shape 创建/拷贝 → 落到 `view/<x>_renderer.{h,cpp}`。
- module.cpp 含对话框 / `QMessageBox` → 落到 `<模块>/commands/` 或 `<模块>/ui/`。

UI 层（widget / dialog / ribbon_*_tab）：
- 业务调用统一经 `Kernel::current().service<IXxxFacade>()` 或 `service<XxxModule>()`。
- 命令触发统一经 `commandContainer()->findAction(name)`，UI 中不 new QAction 业务行为。

## 6. 算法库（core/algorithms/<域>）原则

1. **零 Qt UI 依赖**：不 include `QWidget / QAction / QDialog / QMessageBox`。`QString` 作为字符串容器允许。
2. **零 LcncDocument / GuiDocument / Kernel 依赖**：输入 OCC + 标量 + 可选 `TaskProgress*`；输出 OCC + bool/错误码 + 可选 `QString* errMsg`。
3. **命名空间**：`lcnc::cad_algo::` / `lcnc::cam_algo::` / `lcnc::kinematics::`。
4. **优先自由函数**：参数多用轻量 POD（`struct ToolpathParams { ... }`），不要无谓包装类。
5. **失败约定**：返回 `bool` + 输出参数 + `QString* errMsg`；不抛异常（OCC `Standard_Failure` 由调用方 catch 并 LCNC_ERR）。

## 7. 命令系统约定

1. 命令类继承 `lcnc::CommandBase`，构造函数完成 QAction 装配（图标/快捷键/可选 checkable）。
2. `isEnabled()` 用于 ribbon/menu 灰显判断；不在 execute 内做权限判断。
3. `execute()` 失败统一 `return false`。
4. 命令文件按域归类 `<模块>/commands/commands_<子域>.{h,cpp}`：
   - CAD：`commands_primitives` / `commands_transform` / `commands_boolean` / `commands_measure` / `commands_io`
   - CAM：`commands_machine` / `commands_cam`
   - Process：`commands_process`
5. **公共 helper** 下沉到 `<模块>/commands/command_helpers.{h,cpp}`。

## 8. 资源 & 异常

1. RAII 优先：`std::unique_ptr` 单所有权 / `std::shared_ptr` 多所有权 / OCC `Handle()`。
2. Qt 父子树用 `QObject* parent` 自动释放；非 QObject 走 `unique_ptr`。
3. catch 三级：
   - `catch(const std::exception& e)` → log `e.what()`
   - `catch(const Standard_Failure& f)` → log `f.GetMessageString()`
   - `catch(...)` 兜底 → log "unknown exception"
4. 不要 `goto`、不要在头文件 `using namespace std`、不要在头文件定义非 inline 静态变量。

## 9. 测试 & 验证

每次提交前：
1. **编译**：`cmake --build build --config Debug -- /m /nologo` 零 error。
2. **冒烟**：根据改动域跑对应用例。
3. **依赖体检**：`grep -r '#include "(view|modules|app)/' src/core` 应为 0 命中。

---

## 速查清单（编码前 30 秒）

- [ ] 头文件每个公开接口有一句注释？
- [ ] 用了 `LCNC_<LEVEL>(lcnc::LogCode::Xxx, ...)` 不是裸 spdlog？
- [ ] `core/` 没 include `view/` `modules/` `app/`？
- [ ] 算法函数零 Qt UI 依赖？
- [ ] `module.cpp` 没出现大段 BRep/BRepAlgoAPI/AIS 代码？
- [ ] 命令 `isEnabled()` 与 `execute()` 职责分离？
- [ ] 单例访问统一走 `lcnc::Kernel::current().<getter>()`？
- [ ] catch 块都打了 ERR？
1、头文件每个接口应有注释
2、日志分级，正常模式下只输出加工日志以及异常日志。调试模式下函数入口出口包括所有return处应有日志输出，异常以及必要的位置应添加日志输出以供调试。

## 10. 算法库编码原则（v2.2 新增）

`core/algorithms/<域>/` 下的代码必须遵守如下准则，以保证可复用、可测试、可跨模块共享：

1. **零 Qt UI 依赖**：不 include `QWidget / QAction / QDialog / QMessageBox` 等。`QString` 作为纯字符串容器可用。
2. **零 LcncDocument / GuiDocument / Kernel 依赖**：输入仅 OCC 类型 + 标量 + 可选 `TaskProgress*`；输出 OCC 类型 + 错误码/布尔 + 可选 `QString* errMsg`。
3. **命名空间**：`lcnc::cad_algo::` / `lcnc::cam_algo::` / `lcnc::kinematics::`。
4. **函数级主作业体**：优先自由函数不包装为类；需要多个参数时用轻量 POD（`struct ToolpathParams { ... };`）不要提供过多 OOP 接口。
5. **module 调用约定**：业务编排在 `modules/<x>/<x>_module.cpp` 或 `modules/<x>/commands/`；调算法 → 写回 `LcncDocument` → emit Qt 信号 → UI 刷新。`module.cpp` 不直接持有 OCC 大段代码。

## 4. 架构依赖铁律（v2.2 明确定调）

```
ui (modules/<x>/ui, app/, view/widget_*)
      ↓   （UI 持有模块/facade 指针，调 commands 或 facade）
modules (cad / cam / process)
      ↓   （业务编排：调算法 + 写回 LcncDocument + emit 信号）
view (gui_application / gui_document / widget_occ_view)—仅被 modules / ui 依赖
      ↓
core/algorithms (纯 OCC + 标量，零 Qt UI / 零 LcncDocument / 零 Kernel)
      ↓
core (kernel / command / task / document / settings / logging / kinematics)
```

**铁律**：
- `core/**` 不允许 `#include "view/..."` 或 `#include "modules/..."` 或 `#include "app/..."`。
- `view/**` 不允许 include `modules/` 或 `app/`（`render_quality.h` 是反例已修正）。
- `core/algorithms/**` 函数零 Qt UI 依赖（QString 例外，纯字符串可用）；输入仅 OCC + 标量，输出 OCC + 错误码 + `TaskProgress*`；不依赖 `LcncDocument / GuiDocument / Kernel`。
- 调用方向：UI 调 modules 调 algorithms。跨模块调用走 facade 或 EventBus。
- 进程内唯一全局对象：`lcnc::Kernel`；所有原有 `XxxClass::instance()` 已删除，访问走 `lcnc::Kernel::current().<getter>()`。