# LaserCNC 微内核架构框架

> 版本：v2.2（v2.1 + Phase A/B1/E/F1 完成 — 微内核体检、算法库落位、Process 命令补齐、cam_config 归位 settings/）
> 适用代码：`src/core/kernel/**`、`src/core/algorithms/**`、`src/modules/**`、`src/main.cpp`

## 1. 设计目标

| 目标 | 落地方式 |
|------|----------|
| 解耦核心与业务 | Kernel 只提供"服务定位 + 事件总线 + 模块生命周期"三件事，不持有任何业务状态 |
| 模块化按需加载 | CAD / CAM / Process 各自实现 `IModule`，由 `ModuleRegistry` 拓扑排序后启动；可裁剪 |
| 接口先行 | 核心服务（日志/配置/任务/文档/命令）均以 `IService` 接口暴露，便于替换/Mock |
| 渐进迁移 | 现有 `instance()` 单例签名保留，但其生命周期改由 Kernel 拥有；UI 代码无需大改 |
| 强可观测 | 关键路径（注册/订阅/init/start/stop）按代码规范要求打 DEBUG/INFO/WARN/ERR/CRIT |

## 2. 顶层结构

### 目录布局（Phase 5 重组完成）

```
src/
├── main.cpp
├── core/                       基础设施（无业务）
│   ├── kernel/                 Kernel + ServiceRegistry + EventBus + IService/IModule
│   ├── logging/                spdlog 封装
│   ├── settings/               TomlConfig + AppSettings
│   ├── command/                CommandBase + CommandContainer + ICommandContext
│   ├── task/                   TaskManager（旧 ITaskRunner adapter 已删，直接持有）
│   ├── document/               LcncApplication + LcncDocument + XcafUtils
│   ├── kinematics/             machine_kinematics + ik_solver（v2.2 从 cam/services 迁入）
│   └── algorithms/             【v2.2 新增】纯 OCC 算法库，被各模块共用，零 Qt UI 依赖
│       ├── cad/                （后续 Phase B3：io_step / primitives / boolean_ops / measure）
│       └── cam/                laser_toolpath / face_classifier / machine_model_compressor
├── view/                       共享 OCC 视图基础
│   ├── widget_occ_view
│   ├── gui_application / gui_document
│   ├── render_quality.h        【v2.2 新增】MachineRenderQuality 枚举（曾在 cam_config）
│   └── graphics/               AisXxxFactory
├── modules/                    业务模块（每个独立目录）
│   ├── cad/
│   │   ├── cad_module.{h,cpp}
│   │   ├── commands/           commands_file/edit/cad（后续 Phase D2 拆分）
│   │   ├── services/           shape_service
│   │   └── ui/                 widget_model_tree
│   ├── cam/
│   │   ├── cam_module.{h,cpp}
│   │   ├── commands/           commands_machine/cam
│   │   ├── settings/           cam_config（v2.2 从 services/ 迁入）
│   │   └── ui/                 widget_machine_panel / widget_toolpath_panel / dialog_mark_axes
│   └── process/
│       ├── process_module.{h,cpp}
│       ├── commands/           commands_process（v2.2 新增）
│       ├── settings/           process_settings
│       └── ui/                 widget_laser_control
└── app/                        应用层（外壳）
    ├── main_window.{h,cpp}
    ├── app_context.{h,cpp}
    ├── command_registry.{h,cpp}
    ├── commands/               commands_display
    └── dialog/                 dialog_task_manager
```

```
┌──────────────────── main.cpp ───────────────────┐
│                                                 │
│   QApplication                                  │
│   Logger::init                                  │
│                                                 │
│   ┌──────────── lcnc::Kernel ──────────────┐    │
│   │  ServiceRegistry  (typeid → IService)  │    │
│   │  EventBus         (typeid → handlers)  │    │
│   │  ModuleRegistry   (拓扑 init/start)    │    │
│   │                                        │    │
│   │  Kernel 直接持有（unique_ptr）：       │    │
│   │    AppSettings                         │    │
│   │    LcncApplication                     │    │
│   │    TaskManager                         │    │
│   │                                        │    │
│   │  Kernel 裸指针持有（所有权在 main/UI）：    │    │
│   │    GuiApplication（v2.2：main() 创建，     │    │
│   │                    避免 core→view）     │    │
│   │    CommandContainer（MainWindow 创建）    │    │
│   │                                        │    │
│   │  业务模块（IModule + IService）：       │    │
│   │    CadModule       (id=cad)            │    │
│   │    CamModule       (id=cam, deps=cad)  │    │
│   │    ProcessModule   (id=process,        │    │
│   │                     deps=cam)          │    │
│   └────────────────────────────────────────┘    │
│                                                 │
│   MainWindow（UI 层，按需向 Kernel 取服务）     │
│                                                 │
└─────────────────────────────────────────────────┘
```

模块开关：`mainwindow.toml` 中 `[modules].disabled = ["process"]` 可关停某模块；依赖该模块的下游同步跳过。

## 3. 核心抽象

### 3.1 `IService`（[i_service.h](src/core/kernel/i_service.h)）

```cpp
class IService { public: virtual ~IService() = default; };
```

只是 ServiceRegistry 类型擦除存储所需的标记基类。**所有可被定位的服务必须公开继承之**。

### 3.2 `ServiceRegistry`（[service_registry.h](src/core/kernel/service_registry.h)）

按 `std::type_index(typeid(T))` 注册/查找：

```cpp
auto svc = std::make_shared<MyService>();   // 或共享所有权
kernel.services().registerService<IFoo>(svc);
auto p = kernel.services().getService<IFoo>();   // 未注册返回空
```

非线程安全（注册阶段在主线程，运行时只读）。

### 3.3 `EventBus`（[event_bus.h](src/core/kernel/event_bus.h)）

类型化发布/订阅，事件类型即键：

```cpp
struct DocOpened { DocumentId id; QString path; };
auto sub = bus.subscribe<DocOpened>([](const DocOpened& e){ ... });
bus.publish(DocOpened{id, path});
bus.unsubscribe(sub);
```

- 所有回调在 `publish` 调用线程同步执行；UI 切线程由订阅方负责。
- 互斥锁保护订阅表；publish 取快照后释放锁，避免回调期间持锁。
- 单个订阅者抛异常会被 `try/catch` 隔离并记 ERR，不影响其他订阅者。

### 3.4 `IModule`（[i_module.h](src/core/kernel/i_module.h)）

```cpp
struct ModuleInfo {
    QString     id;            // 进程内唯一短标识
    QString     displayName;
    QString     version;
    QStringList dependencies;  // 依赖的其他模块 id
};

class IModule {
public:
    virtual ModuleInfo info() const = 0;
    virtual bool init(IKernel& kernel) = 0;   // 注册自身服务/命令
    virtual bool start() = 0;                 // 依赖均 init 完成后调用
    virtual void stop() = 0;                  // 反向释放
};
```

### 3.5 `ModuleRegistry`（[module_registry.h](src/core/kernel/module_registry.h)）

- Kahn 拓扑排序（按 id 字典序稳定输出）
- `init` 阶段失败：反向 `stop` 已 init 模块，整体失败；
- `start` 阶段失败：调用 `stopAll()` 反向停止所有已 start 模块；
- 检测循环依赖、缺失依赖时打 ERR 并失败。

### 3.6 `Kernel`（[kernel.h](src/core/kernel/kernel.h) / [kernel.cpp](src/core/kernel/kernel.cpp)）

```cpp
lcnc::Kernel kernel;
kernel.registerCoreServices();           // 1) 创建 AppSettings/LcncApplication/TaskManager
kernel.setGuiApp(std::make_unique<GuiApplication>().release()); // 2) main 注入 GuiApp
kernel.addModule(std::make_unique<CadModule>());
kernel.addModule(std::make_unique<CamModule>());
kernel.addModule(std::make_unique<ProcessModule>());
kernel.bootstrap();                       // 3) 拓扑 init + start
// ... app.exec() ...
kernel.shutdown();                        // 4) 反向 stop + clear
```

- **唯一允许的"全局对象"**：在 `main()` 中按值持有，禁拷贝禁移动。
- 暴露：`services()` / `events()` + 强类型 getter `app() / guiApp() / taskManager() / appSettings() / commandContainer()`。
- v2.2 变更：不再提供 `logger() / settings() / tasks() / documents() / commands()` 这五个 IService 快捷访问（零调用 + 与强类型 getter 负责重复），代以直接访问实例。

## 4. 架构依赖铁律（v2.2 明确定调）

```
ui (modules/<x>/ui, app/, view/widget_*)
      ↓   （UI 持有模块/facade 指针，调 commands 或 facade）
modules (cad / cam / process)
      ↓   （业务编排：调算法 + 写回 LcncDocument + emit 信号）
view (gui_application / gui_document / widget_occ_view)—仅被 modules / ui 依赖
      ↓
core/algorithms (纯 OCC + 标量，零 Qt UI / 零 LcncDocument / 零 Kernel)
      ↓
core (kernel / command / task / document / settings / logging / kinematics)
```

**铁律**：
- `core/**` 不允许 `#include "view/..."` 或 `#include "modules/..."` 或 `#include "app/..."`。
- `view/**` 不允许 include `modules/` 或 `app/`（`render_quality.h` 是反例已修正）。
- `core/algorithms/**` 函数零 Qt UI 依赖（QString 例外，纯字符串可用）；输入仅 OCC + 标量，输出 OCC + 错误码 + `TaskProgress*`；不依赖 `LcncDocument / GuiDocument / Kernel`。
- 调用方向：UI 调 modules 调 algorithms。跨模块调用走 facade 或 EventBus。
- 进程内唯一全局对象：`lcnc::Kernel`；所有原有 `XxxClass::instance()` 已删除，访问走 `lcnc::Kernel::current().<getter>()`。

## 5. 业务模块约定

| 模块 | id | dependencies | 主要职责 |
|------|----|--------------|---------|
| [CadModule](src/modules/cad/cad_module.h)         | `cad`     | —              | 工件文档生命周期、CAD 建模、撤销/重做 |
| [CamModule](src/modules/cam/cam_module.h)         | `cam`     | `[cad]`        | 机台模型、轴运动学、走刀路径生成、仿真、CamConfig |
| [ProcessModule](src/modules/process/process_module.h) | `process` | `[cam]`        | 加工运行（仿真/控制器）、回零/急停、进给倍率 |

每个业务模块当前**同时实现** `IModule + IService + QObject`：
- `IModule` 由 Kernel 调用：`info() / init() / start() / stop()`；
- `IService` 让其他代码可 `kernel.services().getService<CadModule>()` 取到（或便捷封装 `Kernel::current().service<CadModule>()`）；
- `QObject` 信号槽机制不变，UI 层通过 `m_appContext->cadModule()` 或 `Kernel::current().service<...>()` 拿到指针。

### 5.1 单例彻底移除（v2.0 / Phase 6 完成）

`CadModule::instance() / CamModule::instance() / ProcessModule::instance() / LcncApplication::instance() / GuiApplication::instance() / TaskManager::instance() / AppSettings::instance() / CamConfig::instance()` **全部已删除**。

进程范围内访问唯一入口：
```cpp
// 核心对象（Kernel 直接 unique_ptr 持有）
lcnc::Kernel::current().app()              // -> LcncApplication*
lcnc::Kernel::current().taskManager()      // -> TaskManager*
lcnc::Kernel::current().appSettings()      // -> AppSettings*

// 裸指针持有（所有权在 main / MainWindow，v2.2）
lcnc::Kernel::current().guiApp()           // -> GuiApplication*
lcnc::Kernel::current().commandContainer() // -> CommandContainer*

// 业务模块（IModule + IService 双重身份）
lcnc::Kernel::current().service<CadModule>()
lcnc::Kernel::current().service<CamModule>()
lcnc::Kernel::current().service<ProcessModule>()

// 模块持有的子对象
camModule->config()                        // 旧 CamConfig::instance() 替代
```

构造函数级单实例守护：每个上述类的 ctor 内 `Q_ASSERT_X(!s_instance, ...)`，多次构造直接断言，杜绝非 Kernel 通道创建。

### 5.2 命名冲突小结

`ProcessModule` 原有的运行控制方法 `start()/pause()/stop()` 与 `IModule` 的同名生命周期函数冲突，已重命名为 `runStart()/runPause()/runStop()`；UI 信号连接同步更新。`home/emergencyStop/jog/setFeedOverride` 不变。

## 6. 日志规范落地

按 [代码规范.md](代码规范.md)：

| 级别 | 触发条件 | 示例 |
|------|----------|------|
| `LCNC_DEBUG` | 函数入口/出口/关键 return；服务注册查找 | `"CadModule::init begin"` |
| `LCNC_INFO`  | 模块 init/start/stop；启动里程碑          | `"Module init: 'cam' v1.0.0"` |
| `LCNC_WARN`  | 参数无效、覆盖注册、命令未找到/不可用     | `"ServiceRegistry: 'X' already registered, overwriting"` |
| `LCNC_ERR`   | 依赖缺失、循环依赖、模块异常、订阅者抛出 | `"Module 'cam' init threw: ..."` |
| `LCNC_CRIT`  | 未注册核心服务即访问 Kernel getter（同时抛 logic_error） | `"Kernel::logger() called before registerCoreServices()"` |

正常发布构建建议把级别设为 `info`；调试期可经 `ILoggerService::setLevel(spdlog::level::debug)` 全开。

## 7. 启动 / 关闭时序

```
main()
 ├─ QApplication
 ├─ Logger::init       (写日志的最早入口)
 ├─ Kernel kernel
 ├─ kernel.registerCoreServices()       // AppSettings/LcncApplication/TaskManager
 ├─ kernel.appSettings()->loadDefault() // mainwindow.toml
 ├─ GuiApplication guiApp               // 【v2.2】main 创建、入栈接管
 ├─ kernel.setGuiApp(&guiApp)
 ├─ kernel.addModule(CadModule)
 ├─ kernel.addModule(CamModule)
 ├─ kernel.addModule(ProcessModule)
 ├─ kernel.bootstrap()                  // 拓扑 init → start
 │     init order: cad → cam → process
 │     start order: cad → cam → process
 ├─ MainWindow + show
 │     └─ 在 ctor 内创建 CommandContainer 并调 kernel.setCommandContainer
 ├─ app.exec()
 ├─ kernel.shutdown()                   // 反向 stop process → cam → cad，clear services
 ├─ kernel.appSettings()->saveDefault
 └─ 作用域退出：mainWin → guiApp → kernel 依次销毁，Logger::shutdown
```

## 8. 扩展指南

### 8.1 新增一个业务模块

1. 在 `src/modules/foo_module.h` 中：
   ```cpp
   class FooModule : public QObject, public lcnc::IModule, public lcnc::IService {
     Q_OBJECT
   public:
     explicit FooModule(QObject* parent = nullptr);
     static FooModule* instance();
     lcnc::ModuleInfo info() const override;
     bool init(lcnc::IKernel& kernel) override;
     bool start() override;
     void stop() override;
   private:
     static FooModule* s_instance;
     bool m_initialized{false};
   };
   ```
2. `info()` 填好 id / version / dependencies；
3. `init()` 中 `s_instance = this; kernel.services().registerService<FooModule>(svc);`，并连接需要的信号；
4. `stop()` 中清理；
5. `main.cpp` 中 `kernel.addModule(std::make_unique<FooModule>());` 加在 bootstrap 之前；
6. `CMakeLists.txt` 把 `.cpp` 加入 `SRCS_MODULES`。

### 8.2 新增一个核心服务接口

1. 在合适的 `core/<area>/i_xxx_service.h` 定义 `IXxxService : public IService`；
2. 在 `kernel.cpp` 加一个 anonymous namespace adapter；
3. `Kernel::registerCoreServices()` 中注册；
4. 如使用频繁，可在 `IKernel` 加快捷 getter。

### 8.3 模块间通信优先级

1. **事件总线**：跨模块松耦合通知（"机台已加载"、"工件已挂载"）；
2. **服务接口**：跨模块同步查询/调用（`getService<CamModule>()`）；
3. **直接依赖头文件**：仅限同模块内部。

### 8.4 关闭某个业务模块（运行时开关）

在 `mainwindow.toml`（用户级配置，路径：`%APPDATA%/LaserCNC/mainwindow.toml`，
或开发态 `build/Debug/mainwindow.toml`）添加：

```toml
[modules]
disabled = ["process"]
```

`main.cpp` 在 `kernel.addModule()` 之前调用
`AppSettings::isModuleDisabled(id)` 进行过滤；如果一个被关闭的模块还是别人
的依赖（如 `cam` 依赖 `cad`），下游模块也会被同步跳过，并打印
`LCNC_WARN(InternalUnexpectedState, ...)`。

典型用途：
- 调试时临时关闭加工模块，加快启动；
- 为不同硬件出货裁剪功能集合（无激光机型只发 CAD/CAM）；
- 单元测试中按需启用最小模块组合。

## 9. 后续路线

| 阶段 | 状态 | 内容 |
|------|------|------|
| Phase 0 | ✅ | spdlog/toml11 + Logger/AppSettings |
| Phase 1 | ✅ | 文件结构调整 → core/ + view/ |
| Phase 1.5 | ✅ | 微内核骨架 + 核心服务接口 |
| Phase 2 | ✅ | CadModule → IModule + IService |
| Phase 3 | ✅ | CamModule → IModule + IService |
| Phase 4 | ✅ | ProcessModule → IModule + IService |
| Phase 5 | ✅ | 命令注册集中化 + ribbon_*_tab 抽出 + main_window 重命名 |
| Phase 6 | ✅ | CamConfig.json → cam.toml；按模块拆 settings |
| Phase 7 | ✅ | 引入 facade 接口（ICadFacade / ICamFacade / IProcessFacade） |
| **Phase A** | ✅ (v2.2) | 微内核体检：刪 5 IService adapter；GuiApplication 移出 Kernel；清僵尸文件；LogCode 误用修正；异常日志补齐 |
| **Phase B1** | ✅ (v2.2) | `face_classifier / laser_toolpath / machine_model_compressor` 迁 `core/algorithms/cam/`；`ik_solver` 迁 `core/kinematics/` |
| **Phase B2/B3** | ⏳ | CAD 算法抽离：io_step / primitives / boolean_ops / measure → `core/algorithms/cad/` |
| **Phase C**  | ⏳ | `cam_module.cpp` 2069 行 拆分为 io / simulator / renderer / pick / detector ≤900 行 |
| **Phase D**  | ⏳ | `commands_cad.cpp` 923 行 拆为 primitives/transform/boolean/measure/delete；cad_module 瘦身 |
| **Phase E**  | ✅ (v2.2) | Process 补齐 commands （commands_process: runStart/Pause/Stop/EmergencyStop/Reset/Home/Connect/Disconnect/ToggleSim）；ribbon_process_tab 走 commandRegistry |
| **Phase F1** | ✅ (v2.2) | `cam/services/cam_config` → `cam/settings/cam_config`（与 process/settings/ 对齐） |

### 9.1 备注

- **Phase A6 暂缓**：`AppContext` 在 ~30 个命令中使用，收敛成 `IUiContext` 需一次性改 30+ 口，则留到有遇动代码时边走边修。
- **Phase D3 暂不迁**：Undo/Redo 当前直接调 `cadModule->canUndo / undo`，下沉到 `core/command/` 需先引入 `IUndoTarget` 抽象。
- **Facade 渐进迁移路径**：现阶段 UI 仍直接持有 `CadModule*` 等具体指针；新写的代码应优先经 `Kernel::current().service<lcnc::ICadFacade>()` 取门面。每次接触老 UI 文件时顺手迁移一处即可，不必专项重构。
- **持久化目录约定**：所有模块的 toml 配置文件统一落在 `<exeDir>/config/`（`cam.toml`、`process.toml`、`mainwindow.toml`）；新增模块设置类继承 [`lcnc::TomlConfig`](src/core/settings/toml_config.h) 即可复用 `load/save` 与 `lcnc::toml_io` 帮助函数。

## 10. 算法库编码原则（v2.2 新增）

`core/algorithms/<域>/` 下的代码必须遵守如下准则，以保证可复用、可测试、可跨模块共享：

1. **零 Qt UI 依赖**：不 include `QWidget / QAction / QDialog / QMessageBox` 等。`QString` 作为纯字符串容器可用。
2. **零 LcncDocument / GuiDocument / Kernel 依赖**：输入仅 OCC 类型 + 标量 + 可选 `TaskProgress*`；输出 OCC 类型 + 错误码/布尔 + 可选 `QString* errMsg`。
3. **命名空间**：`lcnc::cad_algo::` / `lcnc::cam_algo::` / `lcnc::kinematics::`。
4. **函数级主作业体**：优先自由函数不包装为类；需要多个参数时用轻量 POD（`struct ToolpathParams { ... };`）不要提供过多 OOP 接口。
5. **module 调用约定**：业务编排在 `modules/<x>/<x>_module.cpp` 或 `modules/<x>/commands/`；调算法 → 写回 `LcncDocument` → emit Qt 信号 → UI 刷新。`module.cpp` 不直接持有 OCC 大段代码。
