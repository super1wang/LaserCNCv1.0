# LaserCNC project explorer controller interaction extraction

**版本**：v1.3.20

**日期**：2026-08-03

**状态**：工程树的控件状态遍历和排序快照已从 `MainWindow` 迁入 controller。

## 本次交付范围

- `ProjectExplorerController` 新增轮廓单选/多选、CAD 条目选择和拖动后的
  轮廓排序快照；稳定 contour ID 与旧索引回退语义保持不变。
- `MainWindow` 仍负责通过 CAD/CAM facade 写回选中、排序和显示状态，并保留
  工具路径高亮与参数面板接线；不再直接遍历树控件实现这些 UI 状态操作。
- 扩展 offscreen controller 测试，覆盖轮廓稳定 ID、批量选择、排序快照和
  清空 CAD 选择。

## 验证证据

- 串行 quality `/W4 /WX` 构建：`lcnc_app`、
  `lcnc_project_explorer_controller_test` 通过。
- offscreen CTest：`lcnc_project_explorer_controller_test` 与
  `lcnc_startup_smoke_test`，2/2 通过。
- `git diff --check` 和 `scripts/check_architecture.ps1 -Root .` 通过。

## 后续

- 继续将工程树的节点状态写回和右键菜单协调下沉，并收敛最近文件和其它窗口级
  接线。
- 本交付不改变 CAM/工程持久化 schema，也不替代硬件或长稳验证。
