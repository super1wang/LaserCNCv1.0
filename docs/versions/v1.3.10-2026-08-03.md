# LaserCNC CAD reader service extraction

**版本**：v1.3.10

**日期**：2026-08-03

**状态**：STEP/IGES reader 与 XCAF 回退路径已迁入文档 IO 服务。

## 本次交付范围

- `CadDocumentIoService` 接管 STEP/IGES 的 XCAF reader、命名保留与单形体
  fallback；STL、BREP、STEP、IGES 四种 detached geometry 路径统一通过该服务。
- 删除 `CadModule` 内部重复的 STEP/IGES reader 和已无调用方的显示网格 helper。
- `CadModule` 继续只保留异步任务编排、workspace generation 校验、成功后替换
  当前工程及 GUI 信号投影，既有事务时序未改变。

## 验证证据

- 串行 quality `lcnc_module_cad` 与 `LaserCNC` 构建通过。
- `architecture_checks`、`lcnc_cad_algorithm_contract_test` 和
  `lcnc_startup_smoke_test` 通过；启动冒烟单独复跑通过。

## 后续

- 将 `.lcnc` 工程包读取事务迁入服务，保留成功前不替换活动工程的边界。
- 继续拆分 CAD 建模与选择控制器，并开始 CAM 加工面 pipeline 服务。
