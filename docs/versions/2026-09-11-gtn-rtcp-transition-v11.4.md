# GTN RTCP 轮廓间过渡修正与正常加工（v11.4）

## 请求与实施计划

- 日志 `H:/lasercnc.log`：2026-09-11 10:24:22.383，第二轮廓进入快移首点，CAM X=292.36227119783723、控制器 X=288.7857142857144，误差 3.5765569121228395 mm；B/C 一致。首轮廓 RTCP 校验通过。
- 修复连续 IK 后只更新空程轴值、未将规划姿态世界 TCP/法线更新到已求解姿态的问题。保持工件局部几何，发布与轴值同姿态的世界几何，再生成零位 RTCP 参考。
- 审查空程缓存：不可只复用过渡而丢失连续求解对后继轮廓的轴值更新。
- 按用户请求取消构型派生 RTCP 的专用低速、加速度和强制关激光/气体策略；使用正常刀具/机床限制和数字量加工时序。保留构型派生来源、激活记录、显式允许开关，不伪造 MachineVerified。
- 保留坐标失配停机、急停、限位、跟随误差、配置指纹及设备队列故障锁存。
- 增加数值回归与源契约检查，执行 Debug 构建与相关 CTest。未连接或操纵实机。

## 验证

- Ninja Debug 全目标构建通过；新增的正常加工中文提示回归随后增量重编通过。
- `lcnc_rtcp_reference_transform_test` 已通过，覆盖两份日志数值、100 组载体姿态和连续解缓存恢复。
- 架构、翻译上下文、数字量激光源契约 3/3 通过。
- Ninja ASan 全目标构建通过；Debug/ASan 最终中文资源均重新打包、链接通过。
- ASan 相关回归 25/25 通过（12.61 秒），覆盖 RTCP/缓存、机床拓扑与标定、Process 配置/预检/切割安全、停止/恢复、设备队列及生命周期、快照并发和中文提示。
- 新增中文提示回归曾发现 MainWindow 的 RTCP 消息位于 ProcessModule 翻译上下文；已迁回 MainWindow，并在 Debug/ASan 复测通过。
- 最终 Debug 广泛回归 46/47 通过；唯一未通过项为既有 `lcnc_model_envelope_asset_test` 并行运行 180 秒超时（前一轮同代码测试已在 82.60 秒通过）。随后无构建/其他测试负载下独立复跑通过，耗时 179.80 秒，未调整超时阈值。该测试性能裕量不足，仍存在耗时波动风险，不能宣称并行套件稳定全绿。
- 最终 Debug 启动通过（8.16 秒）；ASan 启动通过（17.96 秒）。ASan 合计 26 个不同测试通过。
- Debug 合计 48 个不同测试最终得到通过结果（含上述独立复跑与启动）；这不是一次全量套件 50/50 通过。
- 未重跑两项既有耗时性能门禁：`lcnc_surface_collision_prefilter_test`、`lcnc_machine_safety_index_test`。不宣称全量 50/50 或实机验收通过。

## 具体行为

- 日志失配点原 RTCP 输入为 `[285.2340664569653,-40,-5.480643831866075]`，配 B=`8.213210701737614`、C=`360`。这是对旧规划姿态世界点错误去除新姿态运动所得。
- 正确零位参考为 `[288.7857142857144,-40,-5.051283406946306]`；正向双转台变换得到世界点 `[292.36227119783723,-40,-5.1337050563559]`，对应 CAM 轴值 `[292.36227119783723,40,5.1337050563559]`。
- `setSolvedRapidWorldGeometry()` 从连续 IK 的同一局部点重建世界几何，重复调用不累乘；不从预测轴反推 TCP，也不修改旋转角/轴方向/容差。
- `m_travelSolutionCache` 补充保存连续求解后的轮廓几何。复用前检查键、机床/求解器身份、轴布局、轮廓序列和点数/偏置；不一致时整体重建。当前元数据及最新异步碰撞证明不被旧缓存覆盖。
- 正常 RTCP 使用刀具直线速度/加速度/jerk、机床配置的轴速度/加速度/jerk，以及原有正常 Group 姿态约束。移除的是额外试运动上限，不是机床运动能力约束。
- Group 初始化统一保持输出关闭，切割由正常 CommandList 数字量激光/气体时序控制。输出失败仍失败停机；不使用未初始化的原生 Pro 激光 API。
- 保留 `bAllowConfigurationDerivedRtcp` 作为显式来源许可；旧的 `fConfigurationDerivedLinearVelocityMax`、`fConfigurationDerivedRotaryVelocityMax` 即使仍在历史设置文件中也不再参与执行，界面不再显示这两项。

## 交付边界

- 本轮未更改现场机床配置、控制器配置文件或已有活动标定记录；已有兼容标定无需因本修正重新生成。
- 未连接 GTN 实体机，未驱动运动或激光。软件测试不代表实机精度、激光时序或正常速度切削验收。
- 更新后需要重新生成 CAM 刀路/空程并重新完成所需碰撞校验，重新连接控制器；不要沿用正在执行的旧快照。
- 新日志版本：`adapter_revision=normal-rtcp-transition-v11.4`。
- Debug 程序：`x64/ninja/Debug/LaserCNC.exe`，20,383,232 字节，2026-09-11 10:55:26。
- Debug SHA-256：`179D086ADF7BD917D99C215F3473EC71700D39B4DB215A9BE73A1A463BBC4310`。
- 工作区既有改动保留；未提交、未推送。
