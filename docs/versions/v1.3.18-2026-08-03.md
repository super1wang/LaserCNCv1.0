# LaserCNC global-generation machining-face capture unification

**版本**：v1.3.18

**日期**：2026-08-03

**状态**：全局生成完成后的加工面捕获已使用 pipeline service 的 candidate 提交。

## 本次交付范围

- 全局生成在 stale-result 检查后收集自动/管材分类面为 `Candidate`，由
  `replaceAutomaticFaces()` 统一处理手动面保留、去重和稳定 ID。
- 删除 CAM 入口中最后一处自动面捕获的手工合并/分配逻辑及过渡 allocator。
- 生成完成后的持久化、显示刷新与 Qt 信号时序保持不变。

## 验证证据

- 串行 quality `lcnc_toolpath_generation_service_test` 和 `LaserCNC` 构建通过。
- `architecture_checks`、服务测试和启动冒烟：3/3 通过。

## 后续

- 评估 `MachineCalibrationService` 对机台加载、轴识别和参考点流程的拆分。
- 继续收敛剩余刀路/机台显示投影。
