# 视图与流程 Ribbon 面板细分

**版本**：v1.3.31

**日期**：2026-08-05

**状态**：将原属"视图"面板的抓取菜单与原属"连接"面板的上料/下料位置动作各自拆分为独立面板，降低面板内控件密度。

## 本次交付范围

- 视图页（`MainWindow::buildViewTab`）：将抓取（snap）菜单从"视图"面板移出，新建独立的"抓取"面板承载，置于视图定向快捷按钮之后；菜单与动作组逻辑不变。
- 流程页（`ribbon_process_tab`）：将"移动到上料位置""移动到下料位置"两个动作从"连接"面板移出，新建独立的"位置"面板承载；连接面板仅保留连接/回零/断开。
- 国际化：新增 `position -> 位置` 中文翻译；`crawl` 复用既有译文。

## 自动化验证

- `git diff --check` 通过。
- `scripts/check_architecture.ps1 -Root .` 通过：未检出废弃文档 API 或 Process 模块 OCC 依赖。
- `cmake --build --preset acs-gtn-debug --parallel 16` 通过（`LaserCNC.exe` 目标已完成）。
- `ctest --test-dir build-cmake --build-config Debug --output-on-failure`：**23/23 通过**，含 `architecture_checks` 层级门禁与 `lcnc_startup_smoke_test`。

## 尚未完成

- 未执行人工 GUI 验收：两个 Ribbon 页的新面板布局与图标仍需在桌面界面中手工确认。
- 本次仅调整面板归属，未改变任何命令行为或运动控制路径。
