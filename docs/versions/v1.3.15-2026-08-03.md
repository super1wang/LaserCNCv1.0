# LaserCNC machining-face persistence rebind extraction

**版本**：v1.3.15

**日期**：2026-08-03

**状态**：加工面持久化重绑的候选解析和签名匹配已迁入 pipeline 服务。

## 本次交付范围

- `MachiningFacePipelineService::rebindFromRecords()` 接收工件 shape 快照，枚举
  `TopoDS_Face` 候选、计算签名、匹配当前持久化记录，并以原 ID 原子替换集合。
- 服务返回缺失记录的 ID 与签名；模块只负责 pipeline 失效、日志、显示刷新和 Qt
  通知，不再实现 OCC face 遍历及签名匹配。
- 服务测试覆盖完整重绑后的 face ID 与持久化签名保持。

## 验证证据

- 串行 quality `LaserCNC` 与 `lcnc_toolpath_generation_service_test` 构建通过。
- 受控直接服务测试通过；`architecture_checks`、服务测试和启动冒烟：3/3 通过。

## 后续

- 将异步自动加工面分离任务的候选提交改经 pipeline 服务。
- 抽离机台标定与剩余刀路/机台显示投影。
