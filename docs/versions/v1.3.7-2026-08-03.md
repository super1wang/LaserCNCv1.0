# LaserCNC CAD STL import transaction extraction

**版本**：v1.3.7

**日期**：2026-08-03

**状态**：CAD STL 导入已迁入文档 IO 服务；其余导入事务继续迁移。

## 本次交付范围

- `CadDocumentIoService` 现在拥有 STL 导入任务、文件读取、取消检查和形体写入。
- `CadModule::importStl()` 保持目标文档选择、模块 task scope、失败时关闭新建文档、
  项目会话更新和显示投影，避免改变用户可见事务语义。

## 验证证据

- `cmake --build --preset quality --parallel 1 --target lcnc_module_cad`：通过。
- `git diff --check` 与 `scripts/check_architecture.ps1 -Root .`：通过。

## 后续

- 迁移 STEP/IGES/BREP 与工程包导入的后台解析、显示网格准备、generation 校验和
  成功后工作区提交，再进入 CAM 加工面 pipeline。
