# LaserCNC CAD display mesh service extraction

**版本**：v1.3.9

**日期**：2026-08-03

**状态**：导入共用的显示网格准备已迁入文档 IO 服务。

## 本次交付范围

- `CadDocumentIoService::prepareDisplayMesh()` 接管 BRep mesh 构建、取消检查与 OCC
  异常转换。
- 工程包、detached geometry 打开及 STEP 导入三条路径改经该服务调用。
- 入口继续拥有 workspace generation 校验、成功提交和 GUI 投影；这些事务语义未改变。

## 验证证据

- 串行 quality `LaserCNC` 构建通过。
- 架构检查、CAD 算法契约和启动冒烟：3/3 通过。

## 后续

- 将 STEP/IGES/工程包 reader 迁入服务并删除 `CadModule` 内剩余的 reader/mesh helper。
