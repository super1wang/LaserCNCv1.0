# LaserCNC Process interactive IO service extraction

**版本**：v1.3.23

**日期**：2026-08-03

**状态**：Process 轴使能和数字 IO 的交互式设备事务已从模块入口下沉。

## 本次交付范围

- 新增 `ProcessInteractiveIoService`，统一轴使能和数字输出的 typed runtime 调用、
  Interactive 优先级、队列拒绝及 GUI completion。
- `ProcessModule` 保持轴/IO 状态缓存和 Qt 信号的权威投影，只消费 service 的结果；
  安全轮询、停止顺序和输出复位语义不变。
- 扩展 Process 专用测试，覆盖轴使能和数字输出的设备线程完成回调。

## 验证证据

- quality `/W4 /WX`：`lcnc_module_process` 与手动运动测试构建通过。
- CTest：手动运动（含交互 IO）、连接服务、运行状态机，3/3 通过。
- `git diff --check` 和 `scripts/check_architecture.ps1 -Root .` 通过。

## 后续

- 继续收敛 Process 运行状态组合和 UI 事件出口。
- 本交付不替代 SimulatorCMHP、ACS/GTN 或真实激光物理硬件验证。
