# LaserCNC asynchronous machining-face commit extraction

**版本**：v1.3.16

**日期**：2026-08-03

**状态**：异步加工面分离的候选提交已统一经 pipeline 服务。

## 本次交付范围

- 后台任务直接产生 `MachiningFacePipelineService::Candidate`；在工件 source
  未变化的前提下，完成回调调用服务的原子自动结果替换。
- 服务保留任务运行期间新增的手动面，完成候选去重、稳定 ID 分配和空结果拒绝。
- `CamModule` 删除重复的合并/去重/ID 分配实现，继续只负责任务生命周期、陈旧
  source 判定、错误信号及后续 pipeline 应用。

## 验证证据

- 串行 quality `lcnc_toolpath_generation_service_test` 和 `LaserCNC` 构建通过。
- `architecture_checks`、服务测试和启动冒烟：3/3 通过。

## 后续

- 评估同步分离路径是否应复用同一 candidate 边界。
- 抽离机台标定与剩余刀路/机台显示投影。
