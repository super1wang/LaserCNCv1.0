# v1.5.1 — 离线仿真与机台碰撞文档同步

日期：2026-08-17

## 本次交付

- 将 v1.5.0 离线机台仿真交付记录完整中文化，并统一使用“碰撞源”“完整机台校验”“冻结沙箱”等项目术语。
- 更新架构说明、项目首页、当前交付状态和待办，使其反映 `cad → cam → {simulation, process}` 模块拓扑、CAM 单一轮廓顺序事实源、独立离线仿真边界和异步完整机台校验。
- 同步 `AGENTS.md` 与 `CLAUDE.md` 的模块启动顺序和 CMake/Ninja 输出约定，避免后续开发重新使用旧的 Insiders 工具链、扁平 `x64/Debug` 输出或过时的三模块拓扑。

## 验证证据

- 对本次文档覆盖范围进行交叉复核：模块依赖、启动顺序、构建输出、碰撞验证边界与 `src/main.cpp`、`CMakeLists.txt`、`BUILD.md`、`CamModule`、`SimulationModule` 的现状一致。
- `git diff --check` 通过。

## 仍需验证

- 本次仅更新文档，不替代 v1.5.0 已记录的 GUI、ASan、长期资源趋势和物理机台碰撞验证。
