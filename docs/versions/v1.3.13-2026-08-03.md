# LaserCNC machining-face pipeline state extraction

**版本**：v1.3.13

**日期**：2026-08-03

**状态**：加工面集合的核心状态已迁入专用 pipeline 服务。

## 本次交付范围

- 新增 `MachiningFacePipelineService`，持有加工面 OCC handle 集合、稳定 ID
  allocator、revision 哈希和 `CamDataManager` 持久化记录快照。
- CAM 的异步 generation stamp 改由该服务计算加工面 revision，持久化改由服务
  生成记录；工作区切换和持久化重绑经服务重置/替换，以保持 ID 单调性。
- 扩展刀路生成服务测试，覆盖 revision 对 manual 状态变化的敏感性、持久化签名、
  reset 与 ID 重置。

## 验证证据

- 串行 quality `lcnc_toolpath_generation_service_test` 和 `LaserCNC` 构建通过。
- `architecture_checks`、刀路生成/加工面 pipeline 测试和启动冒烟：3/3 通过。

## 后续

- 将自动/手动加工面合并、角色校验和重绑候选解析继续下沉到该服务。
- 抽离机台标定与剩余刀路/机台显示投影。
