# LaserCNC machining-face pipeline rule extraction

**版本**：v1.3.14

**日期**：2026-08-03

**状态**：加工面自动/手动合并和角色规则已迁入 pipeline 服务。

## 本次交付范围

- `MachiningFacePipelineService` 接管手动面添加/去重、面删除、自动结果替换、
  保留手动结果、稳定 ID 分配和重复候选过滤。
- 横截面角色变更的“同工件加工面共享边”门禁迁入服务；无效变更仍由模块以原有
  UI 错误信号通知。
- `CamModule` 仅解析拾取面所属工件、编排异步任务、失效 pipeline、持久化/显示
  触发和 Qt 事件，不再实现上述集合规则。

## 验证证据

- 串行 quality `lcnc_toolpath_generation_service_test` 和 `LaserCNC` 构建通过。
- `architecture_checks`、刀路/加工面 pipeline 测试和启动冒烟：3/3 通过。

## 后续

- 将持久化重绑候选解析与异步自动分离提交继续收敛到 pipeline 服务。
- 抽离机台标定与剩余刀路/机台显示投影。
