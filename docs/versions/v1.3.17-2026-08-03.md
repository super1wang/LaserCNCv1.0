# LaserCNC synchronous machining-face commit unification

**版本**：v1.3.17

**日期**：2026-08-03

**状态**：同步与异步加工面分离均已共享 pipeline service 的候选提交边界。

## 本次交付范围

- `separateMachiningFaces()` 直接收集 `Candidate`，由
  `MachiningFacePipelineService::replaceAutomaticFaces()` 处理手动面保留、去重、
  ID 分配和无结果判定。
- 手动选面模式调用相同服务清除过时自动面；不再保留第二套集合规则。
- 同步/异步分离的提交语义统一，`CamModule` 只保留算法结果收集和 pipeline 应用。

## 验证证据

- 串行 quality `lcnc_toolpath_generation_service_test` 和 `LaserCNC` 构建通过。
- `architecture_checks`、服务测试和启动冒烟：3/3 通过。

## 后续

- 收敛全局生成完成时的加工面捕获路径。
- 抽离机台标定与剩余刀路/机台显示投影。
