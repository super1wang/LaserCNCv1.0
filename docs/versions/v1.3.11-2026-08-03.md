# LaserCNC CAM machining-face display projection extraction

**版本**：v1.3.11

**日期**：2026-08-03

**状态**：加工面 AIS 投影已从 CAM 入口迁入专用服务。

## 本次交付范围

- 新增 `CamDisplayProjectionService`，独占加工面 highlight 的 AIS 生命周期、
  手动/自动颜色、边界样式、透明度、Z layer 与 GUI viewer 刷新。
- `CamModule` 只构造不可变显示快照并提供可见性状态；不再持有加工面 AIS map
  或执行具体 viewer 操作。
- 加工面选择、持久化、pipeline invalidation 和 Qt 信号时序保持不变。

## 验证证据

- 串行 quality `lcnc_module_cam` 及 `LaserCNC` 构建通过。
- `architecture_checks`、`lcnc_toolpath_generation_service_test` 与
  `lcnc_startup_smoke_test`：3/3 通过。

## 后续

- 将加工面集合、ID、revision、自动/手动合并及持久化重绑迁入
  `MachiningFacePipelineService`。
- 继续将机台和刀路其余显示投影从 `CamModule` 移出。
