# v1.5.9（2026-08-21）系统文件级审计与文档基线

> 本编号是本次文档交付记录。源码中的产品版本仍存在多源漂移，尚未在本次文档变更中擅自统一。

## 交付范围

- 对自有源码、构建入口、模块依赖、测试和现有文档进行文件级静态审阅。
- 重写根目录的项目介绍、架构、审计、交付状态和待办，使其反映当前 v4 工程包、三域文档、CAM/Process 边界和设备调度现状。
- 更新代码规范与碰撞专项待办的事实入口。
- 删除两个已被当前审计和历史版本记录覆盖的过期临时文档：
  - `docs/structure-closure-progress.md`
  - `docs/extraction_strategy_delivery.md`
- 保留 `docs/versions/` 历史交付记录，不用当前结论回写历史。

## 主要审计结论

- 分层脚本、Process OCC-free 边界、v4 工程包与自动化测试基础总体有效。
- 真实激光 ULTRON 初始化路径存在释放后使用、未初始化变量和缺失返回值，真实设备发布必须阻断。
- ACS/GTN 存在无截止时间的 SDK 等待；三条命令队列共享串行租约，当前 Stop 优先级不是进程级抢占。
- CAD 后台导入直接修改活动 XCAF 文档，需改为离线构造和带 generation 校验的原子提交。
- 全机碰撞仍是离散节点验证，连续扫掠、保守细分和重规划尚未闭环。
- app/UI/Simulation 对具体模块仍有较多依赖，模块 facade 和 target 可见性需要继续收紧。

完整证据、文件定位和优先级见 [AUDIT.md](../../AUDIT.md)，执行清单见 [todo.md](../../todo.md)。

## 本次验证

- `acs-gtn-debug` Ninja 构建通过。
- Debug CTest：35/35 通过。
- `real-laser` preset 重新配置并构建通过，但 MSVC `/W4` 暴露 ULTRON 的 C4456、C4700、C4715 等安全相关警告；该结果是阻断证据，不是实机放行证据。
- 架构分层检查通过。

## 尚未完成的验证

- 未做 GUI 人工验收。
- 未做真实 ACS/GTN/激光器/IO 联调和低速加工。
- 未完成 ASan、Application Verifier、长时间资源增长和全机连续碰撞验证。
