# LaserCNC CAD TaskPanel controller extraction

**版本**：v1.3.19

**日期**：2026-08-03

**状态**：CAD TaskPanel 的预览、快照投影和草图 overlay 交互已脱离 `MainWindow`。

## 本次交付范围

- 新增 `CadTaskPanelController`，集中 primitive / feature / transform 预览，
  selection context、草图列表与 overlay snapshot 的 GUI 投影，以及 overlay 拾取和
  拖动回调。
- `MainWindow` 只保留 controller 创建、现有 Qt 信号接线和轻量转发；不再在入口内
  构造 CAD preview、overlay item 或解释草图手势。
- CAD 命令执行和建模语义仍留在命令层 / CAD facade，未改变既有信号触发时机。

## 验证证据

- 串行 quality 构建：`lcnc_app`、`lcnc_project_explorer_controller_test` 通过，
  编译参数保持 `/W4 /WX`。
- offscreen CTest：`lcnc_project_explorer_controller_test` 与
  `lcnc_startup_smoke_test`，2/2 通过。
- `git diff --check` 与 `scripts/check_architecture.ps1 -Root .` 通过。

## 后续

- 继续下沉 MainWindow 的最近文件、工程树事件路由和窗口级接线。
- 继续评估机台标定和剩余 CAM 显示投影的完整 service 边界；本交付不替代
  ACS、GTN 或真实激光物理硬件验证。
