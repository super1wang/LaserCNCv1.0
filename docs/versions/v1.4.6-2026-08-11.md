# LaserCNC v1.4.6 — 主窗口状态栏与 Ribbon 布局稳定性

日期：2026-08-11

## 实现内容

- 连接设备进度条改为放入状态栏的固定预留槽位；显示和隐藏进度时不再改变状态栏高度或中央三维视窗尺寸。
- 将进度槽位宽度固定为 420px，并使容器宽度显式不小于进度条最小宽度，完整显示常见连接阶段文字且不裁切进度条。
- 解除左右侧栏堆叠页面向主窗口传播的过大最小高度；Ribbon 最小/正常模式切换后，最大化窗口保持在可用屏幕内。
- 将主窗口最小高度限定为 480px，非最大化窗口可正常向下调整高度；中央 OCC 视窗仍保留自身 300px 最小安全高度。

## 已实现验证

- 用户已完成 GUI 验收：设备连接时视窗尺寸稳定，进度条完整显示；Ribbon 双击收起/恢复和普通窗口高度调整均确认通过。
- CMake/Ninja `cmake --build --preset acs-gtn-debug --target LaserCNC --parallel 16` 通过，并重新链接 `x64/Debug/LaserCNC.exe`。
- `scripts/run_quality_gates.ps1 -SkipConfigure -SkipBuild` 通过：`git diff --check`、架构依赖检查及 CTest 26/26（含 SimulatorCMHP SDK 集成和启动冒烟）。

## 验证边界

- 自动化证据覆盖编译、架构和回归测试；不同分辨率、缩放比例、多显示器和 Ribbon 主题下的布局仍应在现场环境按实际屏幕复核。
- SimulatorCMHP 仅为 SDK 模拟器证据，不替代真实 ACS/GTN、激光器、硬接线急停、长时间运行或工艺质量验收。
