# LaserCNC CAD document I/O service foundation

**版本**：v1.3.5

**日期**：2026-08-03

**状态**：CAD 文档生命周期事务已开始下沉；导入/导出任务迁移仍在进行。

## 本次交付范围

- 新增并注册 `CadDocumentIoService`，作为 CAD 项目文档生命周期的唯一事务边界。
- `CadModule` 的新建、保存、关闭改为服务转发，不再直接操作
  `LcncProjectManager` 的对应生命周期 API。
- 服务保留项目包保存与工件 STEP 导出的既有分流规则，未改变文件格式或 UI 信号。

## 验证证据

- `cmake --build --preset quality --parallel 16`：源码编译通过；两个既有 Process
  测试目标的并行链接触发 MSVC `LNK1140` PDB 容量限制后，以串行重链
  `lcnc_process_connection_service_test`、`lcnc_process_status_service_test`、
  `lcnc_app` 与 `LaserCNC` 通过。
- `ctest --test-dir build-cmake-quality -C Debug --output-on-failure`：22/22 通过。
- `git diff --check` 与 `scripts/check_architecture.ps1 -Root .`：通过。

## 后续

- 将 STEP/IGES/STL/BREP 导入、STEP 导出、后台任务取消与显示网格准备整体迁入
  `CadDocumentIoService`，保持“导入成功前不替换活动工程”的事务语义。
- 随后执行 CAM 加工面 pipeline、显示投影与工程树 projection 契约迁移。
