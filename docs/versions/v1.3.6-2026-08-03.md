# LaserCNC CAD STEP export transaction extraction

**版本**：v1.3.6

**日期**：2026-08-03

**状态**：CAD STEP 导出已迁入文档 IO 服务；导入事务迁移继续进行。

## 本次交付范围

- `CadDocumentIoService` 现在拥有 STEP 导出任务创建、OCC writer、取消检查及错误结果。
- `CadModule::exportStep()` 只负责 facade 转发、模块任务跟踪和 UI 错误投影，
  不再持有 `STEPControl_Writer` 业务实现。
- 保持原有 `TaskManager` 生命周期、取消行为和 `operationFailed` UI 信号。

## 验证证据

- 串行 `cmake --build --preset quality --parallel 1 --target lcnc_module_cad LaserCNC`：通过。
- 定向 CTest（架构检查、CAD 算法契约、启动冒烟）：3/3 通过。
- `git diff --check` 与 `scripts/check_architecture.ps1 -Root .`：通过。

## 后续

- 迁移 STEP/IGES/STL/BREP 导入、显示网格准备和“成功前不替换活动工程”提交事务。
