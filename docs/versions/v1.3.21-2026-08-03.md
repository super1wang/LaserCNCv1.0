# LaserCNC project explorer visibility intent extraction

**版本**：v1.3.21

**日期**：2026-08-03

**状态**：工程树节点的可见性意图解析与级联逻辑已从窗口入口迁入 controller。

## 本次交付范围

- `ProjectExplorerController` 根据 CAD、加工面、刀路根/层/轮廓节点生成不可变的
  `VisibilityChange`，并在树控件内完成原有的勾选级联。
- `MainWindow` 只消费该意图并经 CAD/CAM facade 写回实体、草图、加工面和轮廓状态；
  当前轮廓高亮的既有时序保持不变。
- offscreen 测试覆盖刀路根级联、CAD 子树实体收集，以及既有稳定 ID 轮廓选择/排序。

## 验证证据

- 串行 quality `/W4 /WX` 构建：`lcnc_app`、
  `lcnc_project_explorer_controller_test` 通过。
- offscreen CTest：`lcnc_project_explorer_controller_test` 与
  `lcnc_startup_smoke_test`，2/2 通过。
- `git diff --check` 和 `scripts/check_architecture.ps1 -Root .` 通过。

## 后续

- 继续下沉当前节点选择写回和右键菜单协调；最近文件与窗口级接线仍待整理。
- 本交付不改变工程、CAM 或 Process schema，也不替代物理硬件验证。
