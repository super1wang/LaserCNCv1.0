# LaserCNC Process manual-motion service extraction

**版本**：v1.3.22

**日期**：2026-08-03

**状态**：Process 手动运动的 typed executor 事务已从 `ProcessModule` 下沉。

## 本次交付范围

- 新增 `ProcessManualMotionService`，统一处理相对点动、绝对移动、连续点动和
  停止：轴名/使能/急停校验、Interactive/Stop 优先级选择、设备线程 runner 以及
  GUI 线程 completion。
- `ProcessModule` 仅提供运行状态与轴使能快照、接收状态文本并保持 facade；不再
  直接以 lambda 编排上述四类设备队列调用。
- 队列拒绝、执行失败和本地拒绝均向调用方完成并更新既有状态栏语义；Stop 保持
  Stop 优先级。

## 验证证据

- quality `/W4 /WX`：`lcnc_module_process` 与
  `lcnc_process_manual_motion_service_test` 构建通过。
- CTest：手动运动、连接服务、运行状态机，3/3 通过。
- `git diff --check` 和 `scripts/check_architecture.ps1 -Root .` 通过。

## 后续

- 继续将轴使能和数字 IO 的 UI facade 组合迁入专用服务，并保持安全停止序列。
- 本交付仅验证 SDK-free service/queue 行为；不替代 SimulatorCMHP、ACS/GTN 或
  真实激光物理硬件验证。
