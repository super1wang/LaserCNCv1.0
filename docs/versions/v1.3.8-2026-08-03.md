# LaserCNC CAD detached-workspace geometry parsing extraction

**版本**：v1.3.8

**日期**：2026-08-03

**状态**：STL/BREP 解析已从 CAD 入口移入文档 IO 服务。

## 本次交付范围

- `CadDocumentIoService` 提供 STL/BREP 的同步解析入口与异步任务入口。
- `CadModule::openDocument()` 对 detached workspace 的 STL/BREP 读取改为服务调用；
  工作区仍仅在后台解析、网格准备和 generation 校验成功后由原有路径提交。
- 单文档 STL 导入继续经服务执行，保持失败时关闭本次新建文档的行为。

## 验证证据

- 串行 quality 构建 `lcnc_module_cad` 与 `LaserCNC`：通过。
- 架构检查、CAD 算法契约和启动冒烟：通过。

## 后续

- 迁移 STEP/IGES、工程包加载和显示网格准备，随后删除入口内遗留的 reader/mesh helpers。
