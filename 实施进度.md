# LaserCNC Process 模块重构实施进度

审阅日期：2026-05-26  
基线计划：[process模块重构计划.md](process模块重构计划.md)  
框架目标：[process模块框架.md](process模块框架.md)

## 执行原则

- 以 `ProcessFlowDocument` 作为流程唯一事实源，UI 只通过 model/view 展示和编辑。
- `ProcessModule` 不持有 QWidget/QTreeView，不从 UI 树同步业务数据。
- 节点“完整”按新框架功能等价验收：可添加、可编辑、可校验、可保存加载、可执行或给出明确服务缺失反馈、可回写运行状态。
- 真实外设默认 SDK-off 构建完整；真实 adapter 通过 CMake option 启用，SDK 缺失时明确报错。
- 每个阶段完成后立即更新本文，并执行 Debug 构建验证。

## 总体进度

| 阶段 | 目标 | 状态 | 完成度 | 最近验证 |
| --- | --- | --- | --- | --- |
| Phase 1 | 拆除 UI 反向依赖，确立 workflow document 事实源 | 完成 | 100% | Debug 构建通过 |
| Phase 2 | 流程树 UI 完整化：registry、拖放规则、状态反馈 | 完成 | 95% | Debug 构建通过 |
| Phase 3 | 统一参数界面与完整参数 schema/pages | 进行中 | 75% | Debug 构建通过 |
| Phase 4 | 节点 registry 与全节点编辑器 | 完成 | 90% | Debug 构建通过 |
| Phase 5 | 流程执行引擎、按钮控制、节点状态回写 | 完成 | 90% | Debug 构建通过 |
| Phase 6 | 外设接口、仿真闭环、真实 adapter option | 进行中 | 65% | Debug 构建通过 |
| Phase 7 | Cutting/OverCutting 与 CAM dry-run 集成 | 完成 | 80% | Debug 构建通过 |
| Phase 8 | 旧兼容层清理、文档和最终验收 | 进行中 | 70% | Debug 构建通过 |

## 当前差异

- `ProcessModule` 已清除旧 `ProcessTreeView*` 桥接和旧树同步函数。
- 流程树右键菜单、默认参数、Info 摘要、插入/拖放规则已由 `ProcessNodeRegistry` 统一驱动。
- 参数页已覆盖 Axis、Tool、IO、Gas、Water、Monitor、LoadingPos、Camera、Internet 的基础字段，旧 Setting 复杂列表仍待逐项补齐。
- 节点编辑器已覆盖当前所有节点的 typed 基础参数，旧专用复杂 UI 和完整校验仍待逐项迁移。
- 执行器已逐节点异步执行并回写状态；细粒度暂停恢复、条件/循环语义和真实 node executor registry 待增强。
- 设备层已具备 `ILaserDevice`、`IProcessIo`、`IProcessAuxDevice`、仿真激光/IO 和真实 SDK options；真实 adapter 仍待 SDK 路径和协议确认后接入。

## Phase 1 完成记录

**状态**：完成  
**目标**：移除 ProcessModule 对旧 UI 树的依赖，确保新建、加载、保存只操作 `ProcessFlowDocument` / `ProcessFlowStore`。

### 完成项

- [x] 移除 `ProcessModule::m_processTreeView` 和 `setProcessTreeView()` 等旧接口。
- [x] 移除 `ProcessModule` 中旧树同步函数。
- [x] `QG_ProcessesWidget` 直接构造新流程树，不依赖旧 `.ui` 自定义 `ProcessTreeView`。
- [x] CMake 移除旧 `mimeData/treeitem/treemodel/Process_TreeView` 源文件。
- [x] Debug 构建通过。

### 修改文件

- [src/modules/process/process_module.h](src/modules/process/process_module.h)
- [src/modules/process/process_module.cpp](src/modules/process/process_module.cpp)
- [src/modules/process/Process/qg_processeswidget.h](src/modules/process/Process/qg_processeswidget.h)
- [src/modules/process/Process/qg_processeswidget.cpp](src/modules/process/Process/qg_processeswidget.cpp)
- [CMakeLists.txt](CMakeLists.txt)

### 验证

- `cmake --build build --config Debug -- /m /nologo` 通过。
- 仅保留既有 `qrc_resources.cpp.obj : warning LNK4099` PDB 警告。

## Phase 2 完成记录

**状态**：完成  
**目标**：流程树从节点 registry 构建菜单和规则，拖放移动稳定，运行状态反馈正常。

### Phase 2 已完成

- [x] 新增 `ProcessNodeRegistry`，统一注册节点顺序、类别、默认参数、摘要、放置规则和执行器 key。
- [x] 右键新增菜单改为从 registry 生成。
- [x] `ProcessFlowModel` 新增节点时使用 registry 默认参数，Info 列使用 registry 摘要。
- [x] `ProcessFlowDocument::moveNode()` 调用 registry 校验父子放置规则。
- [x] `ProcessFlowDocument::appendNode()` 新增节点时也校验顶层/容器规则。
- [x] `ProcessFlowTreeView::insertionParentId()` 改为 registry 子节点规则。
- [x] 执行器运行时回写节点状态并刷新流程树。
- [x] Debug 构建通过。

### Phase 2 待完成

- [ ] 复杂嵌套流程拖放需要人工 smoke 验收。

## Phase 3 完成记录

**状态**：进行中  
**目标**：参数窗口覆盖工艺、轴、IO、气、水、监控、上料位、相机和外设工具参数。

### Phase 3 已完成

- [x] 保留左树 + 右侧参数页结构。
- [x] `ProcessSettings` 扩展轴、工具、IO、气、水、监控、上料位、相机、联网基础参数。
- [x] 设置对话框新增 Axis、Tool、IO、Gas、Water、Monitor、LoadingPos、Camera、Internet 页面。
- [x] Apply/OK 可持久化新增参数到 `process.toml`。
- [x] Debug 构建通过。

### Phase 3 待完成

- [ ] 迁移旧 Setting 模块所有细分字段和复杂列表项。
- [ ] 抽象参数 schema，避免 UI 直接写业务键名。
- [ ] 保存后通知 `ProcessDeviceManager` 和执行上下文刷新。

## Phase 4 完成记录

**状态**：完成  
**目标**：所有流程节点有 metadata、默认参数、校验、编辑器和执行器 key。

### Phase 4 已完成

- [x] 所有已定义流程节点具备 registry metadata、默认参数、摘要和执行器 key。
- [x] 节点编辑器增加通用参数表；未迁移专用编辑器的节点也能编辑参数并保存。
- [x] AxesMove、Feeding、AutoFocus、Cutting、OverCutting、EnergySwitch、IO、Commands、Monitor、Camera、Measurement、MarkAcquire、Alignment、Loop、RunGroup、If、Compare、Calculation 已有 typed 参数控件。
- [x] Debug 构建通过。

### Phase 4 待完成

- [ ] 继续迁移旧节点专用复杂界面和更完整校验提示。

## Phase 5 完成记录

**状态**：完成  
**目标**：运行、暂停、停止、急停按钮实际控制流程树执行，节点状态和日志反馈正常。

### Phase 5 已完成

- [x] `ProcessWorkflowExecutor` 改为 QTimer 异步顺序推进流程节点。
- [x] Run/Pause/Stop/EStop 接入执行器生命周期。
- [x] 节点开始、完成、失败、暂停会回写 `ProcessNodeState` 并刷新流程树。
- [x] Axis/AxesMove 节点会通过信号请求更新仿真轴位置。
- [x] EnergySwitch/IO 节点会通过信号驱动仿真激光/IO。
- [x] Cutting/OverCutting 会做参数校验并输出 dry-run 反馈。
- [x] 修正空流程或同步失败时 `runStart()` 覆盖成 Running 的状态问题。
- [x] Debug 构建通过。

### Phase 5 待完成

- [ ] 抽出独立 node executor interface 和 executor registry。
- [ ] 相机、测量节点接入真实 service/adapter。
- [ ] 等待/运动剩余时间恢复和更细粒度中断。

## Phase 6 完成记录

**状态**：进行中  
**目标**：仿真外设闭环可用，真实外设通过 option adapter 迁移。

### Phase 6 已完成

- [x] 新增 `ILaserDevice`、`IProcessIo`、`IProcessAuxDevice` 外设接口。
- [x] 新增 `SimulatorLaserDevice` 和 `SimulatorProcessIo`。
- [x] `ProcessDeviceManager` 拥有仿真激光和仿真 IO 实例，并同步激光参数。
- [x] EnergySwitch/IO 节点执行副作用已连接到仿真激光/IO。
- [x] 新增 `LCNC_WITH_ACS`、`LCNC_WITH_GTN`、`LCNC_WITH_BDAQ`、`LCNC_WITH_REAL_LASER` CMake options，默认 OFF。
- [x] Debug 构建通过。

### Phase 6 待完成

- [ ] 真实 Pharos/IPG/Raycus/ACS/GTN/bdaq adapter 按 SDK option 接入。
- [ ] 参数页展示真实设备 unavailable 原因。

## Phase 7 完成记录

**状态**：完成  
**目标**：Cutting/OverCutting 节点具备参数、校验、仿真 dry-run 和 CAM facade 边界。

### Phase 7 已完成

- [x] `ICamFacade` 暴露只读刀路摘要接口。
- [x] `CamModule` 实现刀路轮廓数和点数查询。
- [x] `ProcessWorkflowExecutor` 通过 provider 查询 CAM 刀路快照。
- [x] Cutting 校验 feed/energy，连接仿真激光能量，并输出 CAM dry-run 轮廓/点数反馈。
- [x] 非 dry-run Cutting 在无 CAM 刀路时明确失败。
- [x] OverCutting 校验 length/feed 并输出 dry-run 反馈。
- [x] Debug 构建通过。

### Phase 7 待完成

- [ ] 非 dry-run 切割实控执行和轨迹点插补。
- [ ] 与 CAM 轮廓选择、轮廓 id、工件挂载状态做更严格的运行前校验。

## Phase 8 完成记录

**状态**：进行中  
**目标**：清理旧兼容层，完成文档、构建、分层和 smoke 验收。

### Phase 8 已完成

- [x] 旧轻量流程树兼容源已退出 CMake 构建。
- [x] `process模块框架.md`、`process模块重构计划.md`、`文件结构.md` 已同步当前新框架状态。
- [x] Debug 构建通过。

### Phase 8 待完成

- [ ] 旧源码仍作为参考保留，待真实 adapter/旧页面迁移完成后再清理文件。
- [ ] 补 UI 手工 smoke 记录：流程新增/拖放/双击编辑/保存加载/运行暂停停止。

## 验证命令

```powershell
cmake --build build --config Debug -- /m /nologo
```

每阶段完成后同时执行相关修改文件诊断检查，并记录结果。
