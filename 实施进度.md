# LaserCNC Process 模块重构实施进度

审阅日期：2026-05-29  
基线计划：[process模块重构计划.md](process模块重构计划.md)  
框架目标：[process模块框架.md](process模块框架.md)

## 执行原则

- 以 `ProcessFlowDocument` 作为流程唯一事实源，UI 只通过 model/view 展示和编辑。
- `ProcessModule` 不持有 QWidget/QTreeView，不从 UI 树同步业务数据。
- 节点“完整”按新框架功能等价验收：可添加、可编辑、可校验、可保存加载、可执行或给出明确服务缺失反馈、可回写运行状态。
- 真实外设默认 SDK-off 构建完整；真实 adapter 通过 CMake option 启用，SDK 缺失时明确报错。
- 每个阶段完成后立即更新本文，并执行 Debug 构建验证。

## 2026-05-28 架构重审记录

- 已清除并重新生成 [process模块框架.md](process模块框架.md) 与 [process模块重构计划.md](process模块重构计划.md)。
- 新主线明确 Process 模块职责为：硬件/外设管理、加工参数管理、CAM 点集排序与工具匹配、控制器指令生成、工作流编辑与运行控制。
- 新边界要求 Process 运行数据不依赖 OCC，只消费 CAM 导出的纯数据刀路点集 DTO。
- 不恢复旧工程全局 `Service` 类；改为 `ProcessRuntime` 内部协调器 + 状态机、设备、参数、刀路、指令、工作流、执行等内部服务。
- 后续计划切换为新 Phase 0-11：实施进度重置、截图参数树、UI 字段注册、typed 参数 schema、参数连接外设/工具、runtime 状态机、设备 session、CAM 点集合同、刀路排序/工具匹配、指令 planner/translator、执行服务与最终验收。

## 2026-05-29 外设管理器实施记录

- [x] 新增 `IProcessDevice` 公共外设接口，统一外设类型、可用性、连接状态、能力、状态项和错误查询。
- [x] `ILaserDevice`、`IProcessIo`、`IProcessAuxDevice` 接入公共外设接口；仿真激光器和仿真 IO 开始维护连接状态、异常和状态项。
- [x] `ProcessDeviceManager` 扩展为带外设实例/session 的管理入口，支持默认外设、添加、删除、单个连接/断开、全部连接/断开。
- [x] 默认外设实例为 `PureSimulation` 运动控制器和 `Simulator` 激光器；`SimulatorIO` 作为可添加 IO 外设。
- [x] 新增外设管理独立对话框，左侧显示已添加外设，右侧按外设切换状态和调试页，激光/IO 已有基础调试动作。
- [x] Ribbon 中新增“外设管理”，并将“连接控制器”改为“连接设备”；点击后通过 `TaskManager` 异步连接全部启用外设。
- [x] `IProcessFacade` 新增 `connectDevices()` / `disconnectDevices()`；旧 `connectController(endpoint)` 保留为兼容入口并转向新连接流程。
- [x] Debug 构建通过；首次重试前因运行中的 [build/Debug/LaserCNC.exe](build/Debug/LaserCNC.exe) 占用触发 `LNK1168`，结束进程后构建通过。

## 2026-05-31 外设管理器 UI 调整记录

- [x] 外设管理器左侧从“已添加设备型号列表”调整为固定外设角色：`运动控制器`、`激光器`。
- [x] 运动控制器页内通过型号下拉框切换控制核心，不再用设备型号替代“运动控制器”入口。
- [x] 激光器页内通过型号下拉框切换激光设备，不再用设备型号替代“激光器”入口。
- [x] 运动控制器页嵌入原外设参数 UI：`Setting_MotionControl`、`Setting_Axis`、`Setting_IOIndex`、`Setting_Digital`、`Setting_Analog`。
- [x] 激光器页嵌入原 `Setting_Laser` UI，并保留状态监控和基础调试动作。
- [x] IO 从独立外设入口调整为运动控制器页下的 IO 参数/IO 调试 tab，符合 IO 从属于运动控制器的方向。
- [x] 连接运动控制器时同步连接控制器 IO，断开运动控制器时同步断开 IO；运动控制器状态页显示 IO 连接状态和通道统计。
- [x] Apply/OK 会保存外设 UI 字段到 `uiSetting`，并同步 `motionControllerName`、`laserDeviceName`、`controllerEndpoint` 到 typed settings 和设备管理器。
- [x] 加工设置窗口移除 `外设` 树分支，原外设 `.ui` 参数集中进入独立外设管理器；Ribbon 的“运动参数”“激光参数”改为打开外设管理器对应角色页。
- [x] Debug 构建通过，生成 [build/Debug/LaserCNC.exe](build/Debug/LaserCNC.exe)。

## 2026-05-31 外设管理器 IO 设置与调试调整记录

- [x] 外设管理器运动控制器页删除 `控制器地址` 配置项，控制器地址不再作为外设管理界面的直接编辑项。
- [x] 运动控制器和激光器的 `状态` tab 调整为各自角色页的首页。
- [x] `数字IO`、`模拟IO` 从旧 `.ui` 页面改为自定义 IO 表，可添加、删除和编辑 IO 名、IO 索引、输入/输出方向。
- [x] `ProcessSettings` 新增结构化 `ioTable.digital` / `ioTable.analog` 持久化表，保存自定义数字/模拟 IO。
- [x] `IO调试` 页根据 `IO索引` 预设字段和自定义 IO 表生成控制台；数字 IO 使用红/绿指示灯显示低/高电平，数字输出可点击切换状态。
- [x] IO 控制台使用 `QTimer + QtConcurrent` 异步刷新 IO 状态；仿真 IO 增加互斥保护，避免刷新线程与 UI 写输出并发访问。
- [x] Debug 构建通过；首次链接因运行中的 [build/Debug/LaserCNC.exe](build/Debug/LaserCNC.exe) 占用触发 `LNK1168`，结束进程后重试通过。

## 2026-05-31 系统级机台构型实施记录

- [x] 新增 `MachineConfigurationService`，作为系统级机台构型事实源，启动时加载 `config/machine.toml`。
- [x] 机台配置持久化 `machinePreset`、系统轴定义、硬件轴参数和推荐刀路算法；系统轴定义包含轴名、线性/旋转、父轴、方向和原点，硬件轴参数包含控制器索引、归零索引、限位、低/中/高速、加速度和加加速度。
- [x] `Kernel::registerCoreServices()` 注册 `MachineConfigurationService`，CAM 和 Process 不再互相直接读取轴系作为事实源。
- [x] 文件 Ribbon 的“应用程序选项”新增 `机台构型` 参数页，用于配置机台预设和各轴系相互关系。
- [x] CAM 在配置/识别机台轴系后同步系统机台构型；构型变化时回写当前 `MachineKinematics`，并按构型选择三轴或五轴刀路提取策略。
- [x] Process 初始化和构型变化时读取系统轴系，自动重建运行控制面板中的轴位置与点动布局。
- [x] 外设管理器运动控制器页的 `轴系配置` 调整为硬件参数表，只配置当前构型轴的控制器索引、归零索引、速度、加速度、加加速度和正负限位，不再配置轴绑定关系或扩展轴。
- [x] Debug 构建通过，生成 [build/Debug/LaserCNC.exe](build/Debug/LaserCNC.exe)。

## 2026-05-31 运行控制台与仿真采集实施记录

- [x] 右侧加工控制区移至面板顶部，`运行` / `暂停` / `继续` 互斥显示，`停止` 常驻；移除右侧急停按钮和进给倍率控件。
- [x] 点动控制轴名改为绿/灰双色使能按钮；支持输入点动距离，方向按钮按下即执行一次点动并通过定时器持续发送 jog 请求。
- [x] Ribbon `回零` 按钮移动到 `连接` 面板的 `连接设备` 后方；`ProcessModule::home()` 改为通过 `TaskManager` 异步按 Z 轴优先顺序回零。
- [x] 右侧新增常用数字输出按钮：激光、吹气、夹头、水冷、气泵；点击后写入 `IProcessIo`，激光按钮同步驱动 `ILaserDevice::startLaser/stopLaser`。
- [x] `ProcessModule` 新增轴使能状态、常用 IO 输出状态和后台采集定时器；设备连接成功后启动采集，断开或模块停止时停止采集。
- [x] 运动控制器抽象增加可选轴位置、轴使能和回零状态接口；纯软件仿真控制器实现这些接口。
- [x] `SimulatorCMHP` 改为可在 SDK-off 构建中直接使用的软件仿真运动控制器，便于 Process 运行控制、点动、Home 和 IO 联调。
- [x] `get_errors` 检查本轮修改文件无错误，`cmake --build build --config Debug -- /m /nologo` 通过。

## 2026-05-31 运行日志、ACS 指令与切割流程实施记录

- [x] 右侧运行控制面板调整为 `控制` / `系统日志` 双 tab，状态栏显示当前状态机状态，运行/暂停/继续/停止按钮横向排列。
- [x] `ProcessModule` 新增 `processLogMessage` 信号，统一记录操作、状态切换、加工流程、异常和 IO/采集信息；右侧日志按级别着色显示。
- [x] `IMotionController` 增加可选数字/模拟 IO 和程序缓冲执行接口；`SimulationMotionController` 提供内存 IO 与程序执行仿真。
- [x] `ProcessDeviceCoordinator` 和右侧常用 IO 优先通过运动控制器 IO 读写，保留独立 `IProcessIo` 回退路径。
- [x] `AcsMotionControllerAdapter` 扩展连接、轴位置采集、轴使能、Home buffer、数字/模拟 IO、ACS buffer 装载/编译/运行和程序状态查询；仍由 `LCNC_WITH_ACS` 控制编译。
- [x] `SimulatorCMHP` 在启用 ACS SDK 时切换为 ACS simulator adapter，SDK-off 时继续使用软件仿真，保证默认构建可运行。
- [x] `AcsControllerTranslator` 生成基础 ACS 程序行，`MoveLinear` 输出 `LINE(0,1,2,3,4)/V, x,y,z,a,c,feed`，并处理激光、IO、等待和停止命令。
- [x] `ProcessWorkflowExecutor` 的 Cutting 节点接入 `ProcessWorkflowService::commandBuffer()`；仿真/dry-run 走 `ProcessExecutionService`，真实 ACS 路径下发翻译后的程序缓冲。
- [x] 默认 Debug 构建通过；首次链接因运行中的 [build/Debug/LaserCNC.exe](build/Debug/LaserCNC.exe) 占用触发 `LNK1168`，结束本项目调试进程后重试通过。

## 总体进度

| 阶段 | 目标 | 状态 | 完成度 | 最近验证 |
| --- | --- | --- | --- | --- |
| Phase 0 | 重写实施进度主线和执行记录 | 完成 | 100% | Debug 构建通过 |
| Phase 1 | 参数设置树只保留截图节点 | 完成 | 100% | Debug 构建通过 |
| Phase 2 | 注册全部旧 `.ui` 参数字段 | 完成 | 100% | Debug 构建通过 |
| Phase 3 | typed settings schema 与迁移 | 完成 | 100% | Debug 构建通过 |
| Phase 4 | 参数连接外设、工具和运行服务 | 完成 | 100% | Debug 构建通过 |
| Phase 5 | `ProcessRuntime` 与状态机 | 完成 | 100% | Debug 构建通过 |
| Phase 6 | 设备 registry/session/coordinator | 完成 | 100% | Debug 构建通过 |
| Phase 7 | CAM 到 Process 的 OCC-free 点集合同 | 完成 | 100% | Debug 构建通过 |
| Phase 8 | 刀路排序、过滤和工具匹配 | 完成 | 100% | Debug 构建通过 |
| Phase 9 | 控制器无关指令 planner/translator | 完成 | 100% | Debug 构建通过 |
| Phase 10 | workflow/execution service 重构 | 完成 | 100% | Debug 构建通过 |
| Phase 11 | 清理兼容层、文档和最终验收 | 完成 | 100% | Debug 构建通过 |

## 本轮实施记录

- [x] 已将主线进度切换为最终架构 Phase 0-11。
- [x] 已新增 `ProcessSettingsFieldRegistry`，用于扫描并注册旧 `.ui` 中的可编辑控件字段。
- [x] 参数设置树开始按截图结构重构：可见节点限定为 `Settings/外设/运动控制器/运动轴/I/O索引/激光器` 和 `Settings/加工设置/工具/运动&激光/基础参数/随动/吹气/湿切/监控/上料位`。
- [x] 全部 25 个旧 `.ui` 仍作为后台页面加载和字段注册来源；未在截图中的页面不再生成可见树节点。
- [x] `get_errors` 检查当前修改文件无错误。
- [x] `cmake --build build --config Debug -- /m /nologo` 通过；首次链接因运行中的 `LaserCNC.exe` 被占用触发 `LNK1168`，结束本项目调试进程后重试通过。
- [x] 新增 CAM OCC-free 刀路合同：`ToolpathExportSnapshot/Contour/Point` 与 `ICamToolpathProvider`。
- [x] `CamModule` 通过 provider adapter 导出纯数值刀路点集，避免多 `IService` 继承二义性。
- [x] `ProcessModule` 移除旧 count-only CAM fallback，只消费 `ICamToolpathProvider`。
- [x] 新增 `ProcessStateMachine` 与 `ProcessRuntime`，运行状态转换集中记录并同步旧 facade 状态。
- [x] 新增 typed settings schema/snapshot，并将旧 `.ui` 关键数值字段映射到 typed settings。
- [x] 设置保存后 `reloadDeviceSettings()` 会刷新 runtime typed snapshot 和活动设备上下文。
- [x] 新增 `ProcessDeviceCoordinator`，统一运动、激光、IO 的运行时调用入口。
- [x] 新增 `ProcessToolpathService/Sorter/ToolMatcher`，可从 CAM snapshot 生成 `ProcessJobPlan`。
- [x] 新增 `ProcessCommandBuffer`、`ProcessInstructionPlanner`、PureSimulation/ACS/GTN translator 壳。
- [x] 新增 `ProcessWorkflowService`、`ProcessExecutionService`、`ProcessNodeExecutorRegistry` 骨架，形成 workflow -> job plan -> command buffer -> execution service 的最终框架链路。
- [x] 检查 `src/modules/process/**` 未直接 include `TopoDS/AIS/gp/Geom/BRep/XCAF` 等 OCC 头。
- [x] 第三轮 `cmake --build build --config Debug -- /m /nologo` 通过，生成 [build/Debug/LaserCNC.exe](build/Debug/LaserCNC.exe)。

## 历史基线记录

以下记录保留上一轮 Phase 1-12 的已完成基础框架，后续实现以“总体进度”的新 Phase 0-11 为主线。

## Phase 1 完成记录

**状态**：完成  
**目标**：移除 ProcessModule 对旧 UI 树的依赖，确保新建、加载、保存只操作 `ProcessFlowDocument` / `ProcessFlowStore`。

### 完成项

- [x] 移除 `ProcessModule::m_processTreeView` 和 `setProcessTreeView()` 等旧接口。
- [x] 移除 `ProcessModule` 中旧树同步函数。
- [x] `QG_ProcessesWidget` 直接构造新流程树，不依赖旧 `.ui` 自定义 `ProcessTreeView`。
- [x] CMake 移除旧 `mimeData/treeitem/treemodel/Process_TreeView` 源文件。
- [x] Debug 构建通过。

### 修改文件

- [src/modules/process/process_module.h](src/modules/process/process_module.h)
- [src/modules/process/process_module.cpp](src/modules/process/process_module.cpp)
- [src/modules/process/Process/qg_processeswidget.h](src/modules/process/Process/qg_processeswidget.h)
- [src/modules/process/Process/qg_processeswidget.cpp](src/modules/process/Process/qg_processeswidget.cpp)
- [CMakeLists.txt](CMakeLists.txt)

### 验证

- `cmake --build build --config Debug -- /m /nologo` 通过。
- 仅保留既有 `qrc_resources.cpp.obj : warning LNK4099` PDB 警告。

## Phase 2 完成记录

**状态**：完成  
**目标**：流程树从节点 registry 构建菜单和规则，拖放移动稳定，运行状态反馈正常。

### Phase 2 已完成

- [x] 新增 `ProcessNodeRegistry`，统一注册节点顺序、类别、默认参数、摘要、放置规则和执行器 key。
- [x] 右键新增菜单改为从 registry 生成。
- [x] `ProcessFlowModel` 新增节点时使用 registry 默认参数，Info 列使用 registry 摘要。
- [x] `ProcessFlowDocument::moveNode()` 调用 registry 校验父子放置规则。
- [x] `ProcessFlowDocument::appendNode()` 新增节点时也校验顶层/容器规则。
- [x] `ProcessFlowTreeView::insertionParentId()` 改为 registry 子节点规则。
- [x] 执行器运行时回写节点状态并刷新流程树。
- [x] Debug 构建通过。

### Phase 2 待完成

- [ ] 复杂嵌套流程拖放需要人工 smoke 验收。

## Phase 3 完成记录

**状态**：完成  
**目标**：参数窗口覆盖工艺、轴、IO、气、水、监控、上料位、相机和外设工具参数。

### Phase 3 已完成

- [x] 保留左树 + 右侧参数页结构。
- [x] `ProcessSettings` 扩展轴、工具、IO、气、水、监控、上料位、相机、联网基础参数。
- [x] 设置对话框新增 Axis、Tool、IO、Gas、Water、Monitor、LoadingPos、Camera、Internet 页面。
- [x] `resources.qrc` 嵌入全部 25 个旧 Setting `.ui`，设置对话框新增“原 Setting UI”树节点并直接加载原界面布局。
- [x] `ProcessSettings` 新增 `uiSetting` 嵌套参数表，按 `页面.控件名` 持久化 UI 控件值，并兼容读取旧 `legacySetting` 表。
- [x] Apply/OK 可持久化新增参数到 `process.toml`。
- [x] Debug 构建通过。

### Phase 3 待完成

- [ ] 抽象参数 schema 和校验规则，逐步把镜像字段提升为 typed settings DTO。
- [ ] 保存后通知 `ProcessDeviceManager` 和执行上下文刷新。

## Phase 4 完成记录

**状态**：完成  
**目标**：所有流程节点有 metadata、默认参数、校验、编辑器和执行器 key。

### Phase 4 已完成

- [x] 所有已定义流程节点具备 registry metadata、默认参数、摘要和执行器 key。
- [x] 节点编辑器增加通用参数表；未迁移专用编辑器的节点也能编辑参数并保存。
- [x] AxesMove、Feeding、AutoFocus、Cutting、OverCutting、EnergySwitch、IO、Commands、Monitor、Camera、Measurement、MarkAcquire、Alignment、Loop、RunGroup、If、Compare、Calculation 已有 typed 参数控件。
- [x] Debug 构建通过。

### Phase 4 待完成

- [ ] 继续迁移旧节点专用复杂界面和更完整校验提示。

## Phase 5 完成记录

**状态**：完成  
**目标**：运行、暂停、停止、急停按钮实际控制流程树执行，节点状态和日志反馈正常。

### Phase 5 已完成

- [x] `ProcessWorkflowExecutor` 改为 QTimer 异步顺序推进流程节点。
- [x] Run/Pause/Stop/EStop 接入执行器生命周期。
- [x] 节点开始、完成、失败、暂停会回写 `ProcessNodeState` 并刷新流程树。
- [x] Axis/AxesMove 节点会通过信号请求更新仿真轴位置。
- [x] EnergySwitch/IO 节点会通过信号驱动仿真激光/IO。
- [x] Cutting/OverCutting 会做参数校验并输出 dry-run 反馈。
- [x] 修正空流程或同步失败时 `runStart()` 覆盖成 Running 的状态问题。
- [x] Debug 构建通过。

### Phase 5 待完成

- [ ] 抽出独立 node executor interface 和 executor registry。
- [ ] 相机、测量节点接入真实 service/adapter。
- [ ] 等待/运动剩余时间恢复和更细粒度中断。

## Phase 6 完成记录

**状态**：进行中  
**目标**：仿真外设闭环可用，真实外设通过 option adapter 迁移。

### Phase 6 已完成

- [x] 新增 `ILaserDevice`、`IProcessIo`、`IProcessAuxDevice` 外设接口。
- [x] 新增 `SimulatorLaserDevice` 和 `SimulatorProcessIo`。
- [x] `ProcessDeviceManager` 拥有仿真激光和仿真 IO 实例，并同步激光参数。
- [x] EnergySwitch/IO 节点执行副作用已连接到仿真激光/IO。
- [x] 新增 `LCNC_WITH_ACS`、`LCNC_WITH_GTN`、`LCNC_WITH_BDAQ`、`LCNC_WITH_REAL_LASER` CMake options，默认 OFF。
- [x] 默认运动控制器改为 `PureSimulation`，与 ACS-based `SimulatorCMHP` 明确区分。
- [x] `SimulatorCMHP` 和 `ACS` 由 `LCNC_WITH_ACS` 控制可用性，`GTN` 由 `LCNC_WITH_GTN` 控制可用性。
- [x] `ProcessModule` 改为持有活动 `IMotionController` 实例，并按设置切换注册到 `ServiceRegistry`。
- [x] Debug 构建通过。

### Phase 6 待完成

- [ ] ACS/SimulatorCMHP/GTN adapter 执行 SDK-on 编译、模拟器和硬件验证。
- [ ] 真实 Pharos/IPG/Raycus/bdaq adapter 按 SDK option 接入。
- [ ] 参数页展示真实设备 unavailable 原因。

## Phase 7 完成记录

**状态**：完成  
**目标**：Cutting/OverCutting 节点具备参数、校验、仿真 dry-run 和 CAM facade 边界。

### Phase 7 已完成

- [x] `ICamFacade` 暴露只读刀路摘要接口。
- [x] `CamModule` 实现刀路轮廓数和点数查询。
- [x] `ProcessWorkflowExecutor` 通过 provider 查询 CAM 刀路快照。
- [x] Cutting 校验 feed/energy，连接仿真激光能量，并输出 CAM dry-run 轮廓/点数反馈。
- [x] 非 dry-run Cutting 在无 CAM 刀路时明确失败。
- [x] OverCutting 校验 length/feed 并输出 dry-run 反馈。
- [x] Debug 构建通过。

### Phase 7 待完成

- [ ] 非 dry-run 切割实控执行和轨迹点插补。
- [ ] 与 CAM 轮廓选择、轮廓 id、工件挂载状态做更严格的运行前校验。

## Phase 8 完成记录

**状态**：进行中  
**目标**：清理旧兼容层，完成文档、构建、分层和 smoke 验收。

### Phase 8 已完成

- [x] 旧轻量流程树兼容源已退出 CMake 构建。
- [x] `process模块框架.md`、`process模块重构计划.md`、`文件结构.md` 已同步当前新框架状态。
- [x] Debug 构建通过。

### Phase 8 待完成

- [ ] 旧源码仍作为参考保留，待真实 adapter/旧页面迁移完成后再清理文件。
- [ ] 补 UI 手工 smoke 记录：流程新增/拖放/双击编辑/保存加载/运行暂停停止。

## Phase 9 完成记录

**状态**：完成  
**目标**：迁入真实运动控制器实例基础，优先区分纯仿真、ACS 模拟、ACS 真机和固高 GTN。

### Phase 9 已完成

- [x] CMake 新增 `LCNC_PROCESS_SDK_ROOT`、ACS/GTN/BDAQ/REAL_LASER include/lib cache 变量和存在性检查。
- [x] 新增 `PureSimulation` profile，默认无 SDK 依赖；`SimulatorCMHP` 改为 ACS SDK 模拟模式。
- [x] 新增 ACS adapter 与 `AcsSimulatorCmhpControllerAdapter` 源，公共头不暴露 `ACSC.h`。
- [x] 新增 GTN adapter 源，公共头不暴露 `gts.h`。
- [x] `ProcessDeviceManager::createMotionController()` 根据当前 profile 创建活动控制器。
- [x] `ProcessModule` 在启动、设置刷新和关闭时注册/注销活动 `IMotionController` service。
- [x] SDK-off Debug 构建通过。

### Phase 9 待完成

- [ ] `LCNC_WITH_ACS=ON` 下执行 ACS SDK 编译验证和 simulatorCMHP 运行验证。
- [ ] `LCNC_WITH_GTN=ON` 需要指定 `LCNC_GTN_LIB` 后执行编译和真机联调。

## Phase 10 完成记录

**状态**：完成  
**目标**：提供统一通讯接口模块，支持串口、TCP、HTTP 等模式并有 UI 状态监控与收发记录。

### Phase 10 已完成

- [x] 新增 `src/modules/process/communication/**`，包含 endpoint、channel interface/base、manager、log model 和设备基类。
- [x] 支持 Mock、TCP、HTTP 和 Serial 通道；当前环境 `Qt6::SerialPort` 可用，串口源已参与构建。
- [x] `CommunicationSettingsPage` 提供模式选择、连接配置、连接状态、TX/RX/Info/Error 日志和 payload 发送。
- [x] `ProcessSettingsDialog` External 节点接入 Communication 页，配置持久化到 `process.toml`。
- [x] Debug 构建通过。

### Phase 10 待完成

- [ ] 真实激光器 adapter 改造为继承或组合 `CommunicationDeviceBase`。
- [ ] 通讯日志导出和连接失败诊断细化。

## Phase 11 完成记录

**状态**：完成  
**目标**：阅读旧 Setting UI，并将旧参数完整复制到当前参数模块。

### Phase 11 已完成

- [x] 旧 Setting 目录下 25 个 `.ui` 已全部嵌入 Qt resource。
- [x] `ProcessSettingsDialog` 新增“原 Setting UI”根节点，通过 `QUiLoader` 直接加载原 `.ui` 布局，不编译旧 Setting `.cpp/.h`。
- [x] 旧 `CSwitchWidget` 在运行时替换为当前系统可管理的 `QCheckBox`，避免引入旧自定义控件实现。
- [x] `QLineEdit/QComboBox/QCheckBox/QSpinBox/QDoubleSpinBox/QTextEdit/QPlainTextEdit` 控件自动注册为 UI 参数字段。
- [x] 字段按 `uiSetting.<页面>.<控件名>` 保存到 `process.toml`，并兼容读取历史 `legacySetting`，避免旧 `Settings/Service/DT` 依赖进入新框架。
- [x] Debug 构建通过。

### Phase 11 待完成

- [ ] 逐步把关键 UI 字段整理为 typed schema、默认值、单位、范围和设备刷新事件。
- [ ] 旧表格/列表类控件如需真实业务语义，需单独建 DTO 和编辑器。

## Phase 12 完成记录

**状态**：完成  
**目标**：CAM 轮廓按图层管理，工程树显示图层节点，图层可配置颜色和工具。

### Phase 12 已完成

- [x] `LaserContour` 新增 `layerId`，`LaserToolpath` 新增 `ToolpathLayer` 列表。
- [x] `CamDataManager` 新增默认分层、图层查询、图层启用、图层配置和轮廓分配基础 API。
- [x] 刀路生成后按 `sourceInfo/contourType` 自动建立默认图层并维护图层内 contour id 顺序。
- [x] `ProjectExplorer` 显示为 `CAM 数据 -> 图层 -> 轮廓`，根节点显示图层/轮廓数量。
- [x] 图层勾选会批量启用/隐藏该图层所有轮廓；轮廓勾选仍支持单独控制。
- [x] 双击图层节点弹出图层配置对话框，可改名称、颜色和工具；颜色同步到该图层轮廓 AIS。
- [x] Debug 构建通过。

### Phase 12 待完成

- [ ] 图层配置持久化到项目/CAM 配置并随 `.lcnc` 保存加载。
- [ ] 跨图层拖拽轮廓时同步更新 `layerId`。
- [ ] 图层工具与 Process/Cutting 执行参数联动。

## 验证命令

```powershell
cmake --build build --config Debug -- /m /nologo
```

每阶段完成后同时执行相关修改文件诊断检查，并记录结果。
