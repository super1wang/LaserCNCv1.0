# LaserCNC 审阅与校正实施进度

> 按 `/memories/session/plan.md` 推进。每完成一个阶段更新本文。

## 总览

| 阶段 | 状态 | 当前结果 |
| ------ | ------ | ---------- |
| 阶段 0：补齐审阅准则与术语基线 | 完成 | `代码规范.md`、`微内核架构框架.md` 已更新到 v2.3。 |
| 阶段 1：建立文件级审阅台账 | 完成 | 已创建 `文件级审阅台账.md`，覆盖 `src/**/*.h/cpp`，并记录处置方向。 |
| 阶段 2：core 层审阅与清理 | 完成 | `IAppContext` 已迁到 `app/app_command_context.h`；`core/command` 只保留命令基类和不透明上下文指针。 |
| 阶段 3：view 层审阅与清理 | 完成 | `MachineRenderQuality` / `render_quality.h` 已删除；CAM 配置改用 `RenderQualityPreset`。 |
| 阶段 4：CAD 模块审阅与清理 | 完成 | `DocumentTree*` DTO、`cad_model_tree_adapter`、旧选择解析命名已清理。 |
| 阶段 5：CAM 模块审阅与清理 | 完成 | `LaserContour` 稳定 id 已落地，dense CAM id/order 管理下沉到 `CamDataManager`。 |
| 阶段 6：Process 模块审阅与清理 | 完成 | 执行模块保持独立；命令层改为依赖 `IProcessFacade`。 |
| 阶段 7：app 层审阅与清理 | 完成 | ProjectExplorer 树 roles、图标与填充逻辑已抽到 `project_explorer_tree_utils`。 |
| 阶段 8：构建、依赖与仓库卫生 | 完成 | CMake 本机路径已改为可覆盖 cache root；`.gitignore` 已收敛为目录级规则；第三方已有变更未回退。 |
| 阶段 9：最终文档重写 | 完成 | README、组织结构、模块框架、CAM 框架与 CAD 重构设计已重写，并通过 Markdown 诊断。 |

## 2026-05-07 恢复后重新校正

- 修复恢复后的编译错配：`ToolpathSimulator` 实现重新对齐头文件中的单轴回调模型，移除不存在的 `m_applyAxes` 批量回调用法。
- 修复 `MachineIo` 调用已收敛后的 `LcncDocument::importFromXcafRoots()` 接口；保留 `onShapeLoaded` 回调时，通过导入前后 entry 差异回调新增机台 shape。
- 补齐 `ProcessModule::setAxisPositions()` 头文件声明，使实现与类定义一致。
- 重新同步 README、组织结构、模块框架、CAM 框架与 CAD 重构设计，明确唯一可见 3D view 挂接 Machine `GuiDocument`。
- CMake Tools 构建 `LaserCNC` 通过；仍只有既有 `vc143.pdb` 链接警告。

## 已完成校正

- 删除源码中的旧多文档兼容别名：`projectDocument`、`workspaceGuiDocument`、`ensureProjectDocument`、`isProjectDocument`。
- `IAppContext`、`AppContext`、`CamModule`、`GuiApplication`、`LcncApplication` 改用显式三域访问语义。
- 当前旧 API 搜索结果为 0。
- 中央 3D view 已统一为 workspace `GuiDocument`；Workpiece、Machine、CAM 三域都通过各自 source document 的 AIS registry 进入公共视窗。
- 最近一次 CMake Tools 构建 `LaserCNC` 通过。

## 阶段 1 审阅结论

- `core/**` 未发现 include `view/`、`modules/`、`app/` 的反向依赖。
- `view/**` 未发现 include `modules/`、`app/` 的反向依赖。
- `core/algorithms/**` 未发现实际依赖 UI/document/Kernel；唯一命中为注释说明。
- 当前高优先级问题：`core/command/command_context.h` 仍在 core 层前置声明 GUI 类型，`CAD DocumentTree*` DTO 仍在 CadModule 暴露，`MachineRenderQuality` 仍横跨 view/cam settings，`cam_module.cpp` 和 `main_window.cpp` 仍承担过多协调逻辑。

## 阶段 2 完成记录

- 新增 `src/app/app_command_context.h`，承载 UI 命令上下文接口。
- 删除 `src/core/command/command_context.h`。
- `src/core/command/commands_api.h` 不再 include app/view/module 上下文细节，仅前置声明 `IAppContext`。
- 需要解引用上下文的命令 `.cpp` 显式 include `app/app_command_context.h`。
- CMake Tools 构建 `LaserCNC` 通过；仍只有既有 `vc143.pdb` 链接警告。

## 阶段 3 完成记录

- 删除 `src/view/render_quality.h`。
- `GuiDocument` 渲染质量接口改为 `RenderQualityPreset`。
- `CamConfig` 不再 include view，改用 `core/settings/app_settings.h`。
- `cam.toml` 新字段为 `machineRenderQualityPreset`，读取时兼容旧 `machineRenderQuality`。
- 大小写敏感搜索确认旧 `MachineRenderQuality` 类型和 `render_quality` include 已清空。
- CMake Tools 构建 `LaserCNC` 通过；仍只有既有 `vc143.pdb` 链接警告。

## 阶段 4 完成记录

- 删除 `src/modules/cad/ui/cad_model_tree_adapter.*`，并从 CMake 源列表移除。
- 删除 `CadModule::DocumentTreeNode`、`DocumentTreeDocument`、`documentTreeDocuments()`。
- `ProjectExplorerModel` 直接从 Workpiece document 的 XCAF tree 和 CAD sketch snapshots 派生工件节点。
- `CadSelectionResolver::fromDocumentTreeNode` 改为 `fromProjectExplorerNode`。
- `CadModule::documentTreeChanged` 改为 `workpieceStructureChanged`。
- 旧 `DocumentTree*`/adapter/`fromDocumentTreeNode` 搜索为 0。
- CMake Tools 构建 `LaserCNC` 通过；仍只有既有 `vc143.pdb` 链接警告。

## 阶段 5 完成记录

- `LaserContour` 新增 runtime-stable `contourId`，轮廓重排不再只依赖数组下标作为身份。
- `CamDataManager` 接管 dense CAM runtime 数据的轮廓 id 分配、id 查找和按 id 重排。
- `CamModule` 保留 facade 委托与文档/view 同步，重排后同步 CAM document 并刷新刀路显示。
- `ProjectExplorerNode` 与 `MainWindow` 新增 `contourId` 角色，工程树选择、勾选、重排后的选择恢复优先按稳定 id。
- CAM 模块内旧 `projectDocument/workspaceGuiDocument` 兼容别名搜索为 0。
- CMake Tools 构建 `LaserCNC` 通过。

## 阶段 6 完成记录

- Process 模块内未发现 ProjectExplorer、旧 workspace 或 document 兼容层依赖。
- `IProcessFacade` 补齐运行状态、连接状态、回零、急停与复位接口。
- Process 命令不再直接 include/获取 `ProcessModule`，改为通过 `IProcessFacade` 转发。
- Ribbon/命令注释同步到 facade 语义，执行 tab 仍作为独立工作区保留。
- CMake Tools 构建 `LaserCNC` 通过。

## 阶段 7 完成记录

- 新增 `src/app/project_explorer_tree_utils.h/.cpp`，集中管理 ProjectExplorer item roles、图标映射与树填充。
- `MainWindow::rebuildProjectExplorer` 简化为构建 snapshot 后委托 helper 填充树。
- `ProjectExplorerRoles` 统一了 doc/entry/nodeKey/contourId 等角色编号，避免 MainWindow 私有常量继续膨胀。
- CMake 已加入新 app 源文件。
- CMake Tools 构建 `LaserCNC` 通过；仍只有既有 `vc143.pdb` 链接警告。

## 阶段 8 完成记录

- `CMakeLists.txt` 将 Qt、OpenCASCADE、SARibbon 的本机安装路径整理为 `LCNC_QT6_ROOT`、`LCNC_OCCT_ROOT`、`LCNC_SARIBBON_ROOT` 等 cache 变量。
- OCCT 3rdparty link path 已统一从 `LCNC_OCCT_3RDPARTY_DIR` 派生，减少重复硬编码。
- OCC/SARibbon runtime DLL copy 路径改为 cache 变量，并在目录不存在时输出 CMake warning。
- `.gitignore` 从逐个 build 文件清单收敛为 `build/`、CMake、MSVC、VS 用户态文件的通用忽略规则。
- 审阅确认 `3rd/**` 当前存在大量第三方头文件新增，阶段 8 按依赖落地处理，不回退。
- CMake Tools 构建 `LaserCNC` 通过。

## 阶段 9 完成记录

- 清空并重写 [Readme.md](Readme.md)，输出当前三域 document、ProjectExplorer、`.lcnc`、构建配置和审阅结果。
- 清空并重写 [组织结构.md](组织结构.md)，移除旧唯一 ProjectDocument/WorkspaceGuiDocument 叙述，改为当前 `core/view/modules/app` 分层与三域文档结构。
- 新增 [ModuleReadme.md](ModuleReadme.md)，说明 CAD、CAM、Process 模块接入方式、facade 边界和后续拆分重点。
- 新增 [cam模块框架.md](cam模块框架.md)，说明 Machine/CAM sparse document、`CamDataManager` dense runtime 数据和 Process 衔接。
- 新增 [cad重构设计.md](cad重构设计.md)，说明 CAD 模块重构后的 Workpiece 边界、ProjectExplorer 接入和已清理旧 DTO。
- 新文档已通过 Markdown 诊断；`组织结构.md` 中旧单 ProjectDocument/WorkspaceGuiDocument 残留已清空。
- 最终 CMake Tools 构建 `LaserCNC` 通过。

## 下一步

本轮阶段 0-9 已完成。后续优化重点为继续拆分 `MainWindow` 的 ProjectExplorer controller/presenter、拆分 `cam_module.cpp` 的机台/挂载/标定编排，并替换 `.lcnc` PowerShell zip backend。

## 2026-05-09 五阶段计划：阶段 1 运行稳定性收口

- `LcncProjectPackage::save()` 新增基于 `LcncProjectManifest` 模板的保存 overload，由 `LcncProjectManager` 传入 `LcncProjectSession` 中的项目名、manifest 和 `workpiece().sourceFilePath`。
- `.lcnc` 保存后会把实际写出的 manifest 回写到 `LcncProjectSession`，避免后续保存丢失 `createdUtc`、资源路径和工件源文件路径。
- 保留 PowerShell `Compress-Archive` / `Expand-Archive` 后端；本阶段只补充启动失败、压缩失败、解压失败、manifest 缺失、XCAF 缺失等错误日志，不引入 QuaZip。
- `GuiDocument::rebuildDomain()` 和 `eraseDomain()` 增加 display registry 计数日志，记录 domain rebuild 前后指定域和全局 AIS 对象数量，用于排查跨域显示被误清空的问题。
- 静态回归：旧 `manifest.sourceFilePath = workpieceDocument.filePath()` 写法已清空；`core/**` 和 `view/**` 分层 include 检查未发现新增反向依赖。
- CMake Tools 构建 `LaserCNC` 通过；仍只有既有 `vc143.pdb` 非致命链接警告。
- 待手工 smoke：启动、加载机台、导入 STEP、反向顺序加载、三域节点显隐、CAM 轮廓、保存/打开。

## 2026-05-09 运行时显示重复修复与渲染边界收敛

- `GraphicsScene::eraseShape()` 与 `GuiDocument` 内部 erase helper 增加可选延迟刷新参数；`GuiDocument::rebuildDomain()` 先批量擦除再统一更新 viewer，减少导入 STEP 后 Machine domain 重建时的短暂消失。
- Workpiece 安装路径已取消 Machine document 副本：`CamModule::mountWorkpiece()` 只把 Workpiece 源 document 平移到安装位置，Workpiece 节点显隐/选择直接作用于源 AIS。
- `setWorkpieceInstallPosition()` / `updateWorkpieceInstallLocation()` 只影响 Workpiece domain：重量路径平移源 TopoDS 并重建 Workpiece domain，轻量路径只对 Workpiece AIS 设置临时 location，不重建 Machine/CAM。
- `CamModule::syncCamDocumentContours()` 把刀路轮廓写入 CAM document 后通过 `GuiDocument::rebuildDomain(ProjectDomain::Cam)` 唯一显示；`ToolpathRenderer` 不再创建轮廓主体 AIS，仅保留引入线、法线和预览覆盖物。
- 工程树隐藏 CAM 轮廓时操作 CAM document 中对应的唯一 AIS，避免隐藏后残留另一份 runtime 轮廓。
- CMake Tools 构建 `LaserCNC` 通过；仍只有既有 `vc143.pdb` 非致命链接警告。
