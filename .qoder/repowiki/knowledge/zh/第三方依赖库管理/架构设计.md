- 采用纯头文件（Header-only）或预编译库（lib_3rd）的物理隔离结构，通过 `include_3rd` 目录暴露硬件交互接口（如 `ACSC.h`）和抽象桩代码（如 `BuzzerControl.h`）。
- 集成成熟的开源组件：`spdlog` 负责高性能异步日志记录，`toml11` 处理结构化配置文件解析，`magic_enum` 提供枚举反射能力。
- 依赖方向为单向引入，模块内部不包含业务逻辑，仅作为静态资源仓库供上层应用链接和包含。