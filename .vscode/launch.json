{
  "version": "0.2.0",
  "configurations": [
    {
      // F5 先构建，再启动部署到 x64/Debug 的 ACS+GTN 运行产物。
      "name": "Debug LaserCNC",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}/x64/Debug/LaserCNC.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}/x64/Debug",
      "environment": [],
      "console": "integratedTerminal",
      "preLaunchTask": "CMake: build"
    }
  ]
}
