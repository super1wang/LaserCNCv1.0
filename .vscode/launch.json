{
  "version": "0.2.0",
  "configurations": [
    {
      // F5 先构建，再启动 CMake Tools 实际生成的 Ninja 产物。
      "name": "Debug LaserCNC",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}/cmake-build-sdkcheck/LaserCNC.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}/cmake-build-sdkcheck",
      "environment": [],
      "console": "integratedTerminal",
      "preLaunchTask": "CMake: build"
    }
  ]
}
