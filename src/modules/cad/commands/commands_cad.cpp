#include "modules/cad/commands/commands_cad.h"

// This translation unit intentionally stays as a compatibility anchor for the
// legacy CMake source entry. CAD command implementations now live in focused
// files by domain: sketch, view, primitives, transform, boolean, measure, and
// delete/explode.
