# LaserCNC 文件结构

审阅日期：2026-05-08  
范围：当前工作区源码、资源、构建配置和项目文档。`build/` 为本机构建产物，`3rd/` 为第三方库源码，本文只说明其入口和用途，不展开第三方内部每个头文件。

## 顶层目录

| 路径 | 用途 |
| --- | --- |
| `CMakeLists.txt` | 单 executable 构建入口，声明 Qt、OpenCASCADE、SARibbon、第三方 include、源码分组和运行时 DLL 复制。 |
| `Readme.md` | 项目总览入口，说明技术栈、当前架构、目录导航、构建方式和文档索引。 |
| `文件结构.md` | 当前文件目录结构和源码文件职责说明。 |
| `微内核框架结构.md` | 当前单项目微内核框架结构说明。 |
| `微内核架构框架.md` | 既有架构约束文档，可作为微内核边界和规则的基准。 |
| `代码规范.md` | 代码风格、命名、分层和工程约束。 |
| `组织结构.md` | 历史组织说明，后续应与本文保持一致或逐步合并。 |
| `ModuleReadme.md` | 模块化接入说明。 |
| `cam模块框架.md` | CAM 模块设计说明。 |
| `cad重构设计.md` | CAD 重构阶段性设计记录。 |
| `实施进度.md` | 阶段实施记录。 |
| `文件级审阅台账.md` | 历史文件级审阅记录，部分旧描述需以本文和 `todo.md` 为准。 |
| `todo.md` | 本次审阅发现的问题与后续开发规划。 |
| `src/` | 应用源码。 |
| `resources/` | Qt 资源和 SVG 图标。 |
| `3rd/` | vendored 第三方依赖，目前包含 spdlog 与 toml11。 |
| `build/` | CMake/MSVC 本机构建输出，不作为源码结构基准。 |

## 源码总览

```text
src/
├── main.cpp
├── app/
├── core/
├── modules/
│   ├── cad/
│   ├── cam/
│   └── process/
└── view/
```

## 程序入口

| 文件 | 用途 |
| --- | --- |
| `src/main.cpp` | 进程入口；设置 Qt/OpenGL 格式，初始化日志、Kernel、AppSettings、GuiApplication 和 CAD/CAM/Process 模块，创建 MainWindow 并在退出时保存配置和关闭 Kernel。 |

## app 层

`app` 负责应用装配、顶层 UI、命令注册、工程树和应用级对话框。它可以访问 `modules`、`view`、`core`。

| 文件 | 用途 |
| --- | --- |
| `src/app/app_command_context.h` | UI 命令上下文接口，向命令显式暴露 ProjectManager、三域 document、workspace view 和模块 facade。 |
| `src/app/app_context.h/.cpp` | `IAppContext` 实现，桥接 Kernel、GuiApplication、ProjectManager 和各业务模块。 |
| `src/app/command_registry.h/.cpp` | QAction/Command 的注册和查找中心。 |
| `src/app/main_window.h/.cpp` | 主窗口、Ribbon、中央 OCC view、左右工作区、ProjectExplorer 与模块面板路由。 |
| `src/app/project_explorer_model.h/.cpp` | 从 Workpiece/Machine/CAM 三域 document 和 CAM runtime 数据构建工程树 snapshot。 |
| `src/app/project_explorer_tree_utils.h/.cpp` | ProjectExplorer 的 item role、节点填充、图标和 tree helper。 |
| `src/app/commands/commands_display.h/.cpp` | 应用级显示命令，如视图模式和渲染设置入口。 |
| `src/app/dialog/dialog_options.h/.cpp` | 应用选项对话框，管理渲染、机台/CAM 配置等设置入口。 |
| `src/app/dialog/dialog_task_manager.h/.cpp` | 任务管理器对话框，展示后台任务状态。 |

## core 层

`core` 是稳定内核层，不依赖 `view/`、`modules/`、`app/`。其中 `core/algorithms` 应保持纯 OCC/数学算法，不管理 document 生命周期和 UI。

### core/kernel

| 文件 | 用途 |
| --- | --- |
| `src/core/kernel/i_service.h` | 服务基接口。 |
| `src/core/kernel/i_module.h` | 模块基接口和 `ModuleInfo`，声明模块 id、版本和依赖。 |
| `src/core/kernel/i_kernel.h` | Kernel 抽象接口，暴露 ServiceRegistry 和 EventBus。 |
| `src/core/kernel/service_registry.h/.cpp` | 类型化服务注册表，模块 facade 和核心服务通过它注册/查询。 |
| `src/core/kernel/event_bus.h/.cpp` | 事件总线基础设施，用于跨模块广播弱耦合事件。 |
| `src/core/kernel/module_registry.h/.cpp` | 模块注册、依赖拓扑排序、init/start/stop 生命周期管理。 |
| `src/core/kernel/kernel.h/.cpp` | 进程内唯一微内核入口；直接拥有 AppSettings、LcncProjectManager、TaskManager，接收外部注入的 GuiApplication 和 CommandContainer。 |

### core/project

| 文件 | 用途 |
| --- | --- |
| `src/core/project/project_types.h` | 项目域、DocumentId、dirty flags 等公共项目类型。 |
| `src/core/project/project_save_options.h` | `.lcnc` 保存选项。 |
| `src/core/project/lcnc_project_manifest.h/.cpp` | `project.toml` manifest 数据模型和 TOML 读写。 |
| `src/core/project/lcnc_project_package.h/.cpp` | `.lcnc` package 保存/加载；当前用 staging 临时目录和 PowerShell zip 后端。 |
| `src/core/project/lcnc_project_session.h/.cpp` | 单项目运行态 metadata：项目名、路径、manifest、dirty flags、Workpiece/Machine/CAM 项目状态。 |
| `src/core/project/lcnc_project_manager.h/.cpp` | 唯一项目生命周期入口；拥有 Workpiece/Machine/CAM 三份 `LcncDocument`，负责新建、打开、保存、导入、导出和 domain change 通知。 |

### core/document

| 文件 | 用途 |
| --- | --- |
| `src/core/document/lcnc_document.h/.cpp` | OCC/XCAF 数据容器；保存 shape、entity tag、undo/redo 和 document-local tree snapshot。当前仍临时持有 MachineKinematics，后续应继续瘦身。 |
| `src/core/document/xcaf_utils.h/.cpp` | XCAF label、entry、name 等工具函数。 |

### core/kinematics

| 文件 | 用途 |
| --- | --- |
| `src/core/kinematics/i_motion_controller.h` | 运动控制器抽象接口，供 Process 和控制器实现使用。 |
| `src/core/kinematics/machine_kinematics.h/.cpp` | 机床轴定义、轴系树、工件挂载、轴变换和 WPC 变换计算。 |
| `src/core/kinematics/machine_pose.h/.cpp` | 机台当前姿态/轴位置状态。 |
| `src/core/kinematics/ik_solver.h/.cpp` | 五轴运动学逆解接口和实现。 |

### core/algorithms/cad

| 文件 | 用途 |
| --- | --- |
| `src/core/algorithms/cad/primitives.h/.cpp` | 盒、圆柱、球、圆锥、圆环等基础体生成。 |
| `src/core/algorithms/cad/boolean_ops.h/.cpp` | CAD 布尔并、差、交算法封装。 |
| `src/core/algorithms/cad/measure.h/.cpp` | 距离、角度、面积等测量算法。 |
| `src/core/algorithms/cad/sketch.h/.cpp` | 草图几何构建和草图元素计算。 |
| `src/core/algorithms/cad/sketch_constraints.h/.cpp` | 草图约束计算。 |
| `src/core/algorithms/cad/features.h/.cpp` | 拉伸、旋转等特征建模算法。 |
| `src/core/algorithms/cad/transform_ops.h/.cpp` | 平移、旋转、缩放等形体变换算法。 |

### core/algorithms/cam

| 文件 | 用途 |
| --- | --- |
| `src/core/algorithms/cam/face_classifier.h/.cpp` | CAM 面分类算法。 |
| `src/core/algorithms/cam/laser_toolpath.h/.cpp` | 激光刀路核心数据结构和生成算法。 |
| `src/core/algorithms/cam/machine_model_compressor.h/.cpp` | 机台模型压缩/代理几何生成算法。 |

### core/settings、logging、task、command

| 文件 | 用途 |
| --- | --- |
| `src/core/settings/toml_config.h/.cpp` | TOML 配置文件基类。 |
| `src/core/settings/app_settings.h/.cpp` | 应用级设置、渲染 profile、模块禁用列表等配置。 |
| `src/core/logging/log_codes.h` | 日志码枚举。 |
| `src/core/logging/logger.h/.cpp` | spdlog 初始化、封装和关闭。 |
| `src/core/task/task_progress.h/.cpp` | 后台任务进度状态。 |
| `src/core/task/task_manager.h/.cpp` | 后台任务调度、完成通知和任务状态管理。 |
| `src/core/command/commands_api.h/.cpp` | 命令基础 API 和命令容器基础实现。 |

## view 层

`view` 是 OCC 显示基础设施层，可依赖 `core`，不依赖 `modules` 或 `app`。当前唯一可见视图是 workspace `GuiDocument`。

| 文件 | 用途 |
| --- | --- |
| `src/view/graphics_scene.h/.cpp` | 封装 OCC `V3d_Viewer`、`AIS_InteractiveContext`、OpenGL driver、灯光、背景、shape 显示/擦除/选择基础操作。 |
| `src/view/gui_application.h/.cpp` | GUI 应用层对象；拥有唯一 workspace `GuiDocument`，应用全局显示模式和渲染设置。 |
| `src/view/gui_document.h/.cpp` | workspace scene/context/view 管理者；以 `DocumentId + entry` 注册 AIS 对象，支持按 Workpiece/Machine/CAM domain 局部刷新。 |
| `src/view/widget_occ_view.h/.cpp` | Qt OCC 视图 widget，负责 native window attach、鼠标交互、选择、view cube 和视图操作。 |
| `src/view/rendering_manager.h/.cpp` | 渲染 profile 应用器，统一设置颜色、材质、透明度、边线等显示风格。 |
| `src/view/shape_object_driver.h/.cpp` | AIS_Shape 创建和默认显示样式辅助。 |
| `src/view/toolpath_renderer.h/.cpp` | CAM 轮廓、引刀、法向和刀路 AIS 渲染。 |
| `src/view/machine_guide_renderer.h/.cpp` | 机台轴线、导引、刀头方向等辅助渲染。 |
| `src/view/world_axes_renderer.h/.cpp` | 世界坐标轴渲染。 |
| `src/view/sketch_overlay_renderer.h/.cpp` | CAD 草图 overlay 渲染。 |
| `src/view/transform_gizmo_renderer.h/.cpp` | 变换 gizmo 渲染。 |

## modules/cad

CAD 模块负责 Workpiece 域：工件导入/导出、CAD 建模、选择、草图、特征和 CAD 命令。

| 文件 | 用途 |
| --- | --- |
| `src/modules/cad/i_cad_facade.h` | CAD facade contract。 |
| `src/modules/cad/cad_module.h/.cpp` | CAD 模块实现和 facade；编排导入/导出、建模、选择、显示刷新和命令服务。 |
| `src/modules/cad/document/cad_document_state.h/.cpp` | Workpiece document 相关 CAD runtime 状态。 |
| `src/modules/cad/document/cad_document_registry.h/.cpp` | 按 DocumentId 管理 CAD 状态和 SketchManager。 |
| `src/modules/cad/selection/cad_selection.h` | CAD 选择上下文和选择项数据结构。 |
| `src/modules/cad/selection/cad_selection_resolver.h/.cpp` | 从 view/工程树选择解析 CAD 语义选择。 |
| `src/modules/cad/services/cad_modeling_session.h/.cpp` | CAD 建模会话状态。 |
| `src/modules/cad/services/shape_service.h/.cpp` | shape 写入、删除、替换等 document 操作服务。 |
| `src/modules/cad/services/sketch_manager.h/.cpp` | 草图创建、编辑、提交、结束和状态管理。 |
| `src/modules/cad/services/sketch_types.h` | 草图工具、元素和参数类型。 |
| `src/modules/cad/sketch/sketch_serializer.h/.cpp` | 草图数据与 XCAF/document metadata 的序列化辅助。 |
| `src/modules/cad/task/icad_tool_command.h` | CAD 工具命令接口。 |
| `src/modules/cad/task/cad_command_request.h` | CAD 工具请求数据。 |
| `src/modules/cad/task/cad_tool_descriptor.h` | CAD 工具描述符。 |
| `src/modules/cad/task/cad_tool_registry.h/.cpp` | CAD 工具注册表。 |
| `src/modules/cad/task/cad_tool_filter.h/.cpp` | CAD 工具按上下文过滤。 |
| `src/modules/cad/task/cad_command_dispatcher.h/.cpp` | CAD 工具请求分发和核心算法调用。 |
| `src/modules/cad/commands/command_helpers.h/.cpp` | CAD 命令共享 helper，例如当前 document、选择、AIS 查找。 |
| `src/modules/cad/commands/commands_file.h/.cpp` | 打开、保存、导入、导出等文件命令。 |
| `src/modules/cad/commands/commands_edit.h/.cpp` | 编辑类命令。 |
| `src/modules/cad/commands/commands_cad.h/.cpp` | CAD 命令声明和兼容翻译单元。 |
| `src/modules/cad/commands/commands_cad_primitives.cpp` | 基础体建模命令。 |
| `src/modules/cad/commands/commands_cad_boolean.cpp` | 布尔建模命令。 |
| `src/modules/cad/commands/commands_cad_transform.cpp` | 平移、旋转、缩放等变换命令。 |
| `src/modules/cad/commands/commands_cad_measure.cpp` | 测量命令。 |
| `src/modules/cad/commands/commands_cad_delete.cpp` | 删除、拆解等 shape 操作命令。 |
| `src/modules/cad/commands/commands_cad_sketch.cpp` | 草图模式和草图工具命令。 |
| `src/modules/cad/commands/commands_cad_view.cpp` | CAD 视图相关命令。 |
| `src/modules/cad/ui/ribbon_cad_tab.h/.cpp` | CAD Ribbon 页。 |
| `src/modules/cad/ui/widget_cad_task_panel.h/.cpp` | CAD 参数/任务面板。 |

## modules/cam

CAM 模块负责 Machine + CAM 域，并只读使用 Workpiece 域完成挂载、刀路和仿真编排。

| 文件 | 用途 |
| --- | --- |
| `src/modules/cam/i_cam_facade.h` | CAM facade contract。 |
| `src/modules/cam/cam_module.h/.cpp` | CAM 模块实现；机台加载、轴标定、工件挂载、刀路生成、机台/CAM 显示同步。 |
| `src/modules/cam/contracts/cam_data_contracts.h` | CAM runtime 数据 contract，如轮廓稳定 id 等跨层数据结构。 |
| `src/modules/cam/settings/cam_config.h/.cpp` | CAM 和机台相关持久化配置。 |
| `src/modules/cam/services/cam_data_manager.h/.cpp` | 密集 CAM runtime 数据管理，如刀路、轮廓 stable id、重排和 dirty 状态。 |
| `src/modules/cam/services/toolpath_simulator.h/.cpp` | 刀路仿真和轴位置回放。 |
| `src/modules/cam/services/machine_axis_detector.h/.cpp` | 从机台模型推断轴/组件关系。 |
| `src/modules/cam/services/machine_io.h/.cpp` | 机台模型导入/导出和 XCAF 读写辅助。 |
| `src/modules/cam/services/reference_pick.h/.cpp` | 参考点/参考几何拾取。 |
| `src/modules/cam/commands/commands_machine.h/.cpp` | 机台加载、导出、轴标定、挂载等机台命令。 |
| `src/modules/cam/commands/commands_cam.h/.cpp` | CAM 刀路、轮廓、仿真等命令。 |
| `src/modules/cam/ui/ribbon_cam_tab.h/.cpp` | CAM Ribbon 页。 |
| `src/modules/cam/ui/widget_machine_panel.h/.cpp` | 机台和轴设置面板。 |
| `src/modules/cam/ui/widget_toolpath_panel.h/.cpp` | 刀路参数、轮廓和仿真面板。 |
| `src/modules/cam/ui/dialog_mark_axes.h/.cpp` | 机台轴标记对话框。 |
| `src/modules/cam/ui/dialog_axis_calibration_wizard.h/.cpp` | 轴标定向导。 |

## modules/process

Process 模块负责加工执行和仿真控制，左侧执行 tab 独立保留，不进入 ProjectExplorer 树。

| 文件 | 用途 |
| --- | --- |
| `src/modules/process/i_process_facade.h` | Process facade contract。 |
| `src/modules/process/process_module.h/.cpp` | Process 模块实现；连接状态、仿真状态、轴位置、运行/暂停/停止/回零/急停等。 |
| `src/modules/process/controllers/simulation_motion_controller.h/.cpp` | 仿真运动控制器，实现 `IMotionController`。 |
| `src/modules/process/device/process_device_manager.h/.cpp` | Process 外设 descriptor registry 和活动仿真外设管理。 |
| `src/modules/process/device/i_laser_device.h` | 激光器统一接口。 |
| `src/modules/process/device/i_process_io.h` | 加工 IO 统一接口。 |
| `src/modules/process/device/i_process_aux_device.h` | 气、水、监控等辅助外设统一接口。 |
| `src/modules/process/device/simulator_laser_device.h/.cpp` | 仿真激光器实现。 |
| `src/modules/process/device/simulator_process_io.h/.cpp` | 仿真数字/模拟 IO 实现。 |
| `src/modules/process/execution/process_workflow_executor.h/.cpp` | 流程树执行器，按节点顺序推进、回写状态并发出外设/CAM dry-run 请求。 |
| `src/modules/process/settings/process_settings.h/.cpp` | Process 执行设置持久化。 |
| `src/modules/process/workflow/process_node_type.h/.cpp` | 流程节点类型、状态枚举和字符串转换。 |
| `src/modules/process/workflow/process_node.h/.cpp` | 流程节点数据对象，包含 stable id、参数和子节点。 |
| `src/modules/process/workflow/process_flow_document.h/.cpp` | UI 无关流程树事实源。 |
| `src/modules/process/workflow/process_flow_store.h/.cpp` | 流程 TOML 读写和旧 `Process.items` 兼容加载。 |
| `src/modules/process/workflow/process_node_registry.h/.cpp` | 节点 metadata、默认参数、摘要、父子规则和执行 key 注册表。 |
| `src/modules/process/commands/commands_process.h/.cpp` | 加工执行命令。 |
| `src/modules/process/ui/ribbon_process_tab.h/.cpp` | Process Ribbon 页。 |
| `src/modules/process/ui/widget_laser_control.h/.cpp` | 加工/激光控制面板。 |
| `src/modules/process/ui/process_flow_model.h/.cpp` | `ProcessFlowDocument` 的 Qt model adapter，支持状态显示和拖放 MIME。 |
| `src/modules/process/ui/process_flow_tree_view.h/.cpp` | 左侧执行流程树 view，支持右键、拖放、双击编辑和文件操作。 |
| `src/modules/process/ui/process_node_edit_dialog.h/.cpp` | 流程节点编辑器，提供 typed 参数页和通用参数表兜底。 |
| `src/modules/process/Setting/process_settings_dialog.h/.cpp` | 左树右页加工参数对话框。 |
| `src/modules/process/Process/qg_processeswidget.h/.cpp` | 左侧“执行”页容器，挂载新流程树 model/view。 |

## resources

| 文件 | 用途 |
| --- | --- |
| `resources/resources.qrc` | Qt resource collection，注册应用图标和 Ribbon/按钮 SVG。 |
| `resources/icons/*.svg` | UI 图标资源；文件名与命令/工具语义对应，如 `open.svg`、`save.svg`、`workpiece.svg`、`machine.svg`、`toolpath.svg`、`wireframe.svg`、`shaded.svg`、`box.svg`、`cylinder.svg`、`simulate.svg` 等。 |

## 3rd

| 路径 | 用途 |
| --- | --- |
| `3rd/spdlog/` | vendored spdlog 日志库。 |
| `3rd/toml11/` | vendored toml11 配置解析库。 |
